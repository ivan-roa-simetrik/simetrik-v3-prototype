# Documentación del prototipo — Simetrik Agéntico

Este directorio documenta las decisiones de UX/UI que se van tomando a medida que se itera el prototipo con `/simetrik-ui prototype`. No es una spec de producción (eso lo produce `handoff` una vez validada la experiencia) — es el registro vivo de *por qué* el prototipo se ve y se comporta como se ve, para no perder contexto entre sesiones.

Un archivo por sección. Se actualiza en cada iteración, no se reescribe desde cero.

## Secciones documentadas

| Sección | Archivo | Qué cubre |
|---|---|---|
| Login | [`login.md`](./login.md) | Layout split light/dark, referencia visual usada, excepción a Ley 6 |
| Home | [`home.md`](./home.md) | Layout general post-login, estado inicial, relación sidebar/chat/artefacto |
| Sidebar | [`sidebar.md`](./sidebar.md) | Jerarquía de navegación, estructura de Proyectos/Apps/Agents, comportamiento collapse |
| Chat | [`chat.md`](./chat.md) | El eje central: estados, composer completo, selector de proyecto, generación de apps (pendiente de implementar), microinteracciones |
| Panel lateral del chat | [`chat-side-panel.md`](./chat-side-panel.md) | Componente propio dentro del chat (no confundir con el sidebar de navegación): open picker con buscador/filtros por Project/App/Agent, sistema de tabs, posición del botón "+", alineación dinámica del popover, bugs encontrados y corregidos |
| Search | [`search.md`](./search.md) | Modal command-palette, tabs de filtro, bug de aria-label encontrado y corregido |
| Proyecto (concepto) | [`proyecto.md`](./proyecto.md) | No es una pantalla — modelo conceptual de la entidad Proyecto: organización, administrador de cuenta, invitaciones, roles y permisos scoped por proyecto |
| Apps (concepto + vista) | [`apps.md`](./apps.md) | Modelo conceptual de la entidad App (artefacto persistido construido conversacionalmente, no una integración externa; relación muchos a muchos con Proyectos; relación con Chats) más su estado de implementación real — vista `#appsView` construida el 2026-08-14, ver también `home.md` → sección "Apps" |
| Chats and Tasks | [`chats-and-tasks.md`](./chats-and-tasks.md) | Vista global de todos los chats de la cuenta (con o sin contexto de Project/App/Agent), abierta desde el nav item del sidebar — selección masiva con Archive, ellipsis por fila. El concepto de "Task" todavía no está definido |
| Project View Map | [`map.md`](./map.md) | La pantalla completa: canvas con pan/zoom, los 3 tipos de nodo (card de 2 capas, estados completo/colapsado, handles, aristas), fase por fase (Fase 0 empty state, Fase 1 navegación, Fase 2 nodos) |
| Project View Map (concepto) | [`project-map.md`](./project-map.md) | No es una pantalla — modelo conceptual de los nodos del mapa de un proyecto: Integración (con dirección entrada/salida), Dataset (sin variantes, tags libres) y Function; el Ruleset queda fuera del mapa como nodo |
| Supabase (persistencia) | [`supabase.md`](./supabase.md) | No es una pantalla — capa de persistencia real (Postgres/Supabase) para lo que hoy vive en memoria: esquema (organizations/projects/apps/chats/agents/map), RLS, login real. Fase 0 (esquema + auth) completa; Fases 1-6 (lectura/escritura real, mensajes, Realtime, mapa, RLS fino) pendientes |

**Nota de idioma (2026-08-12):** por instrucción explícita del usuario, todo el copy del prototipo (login, home, sidebar, chat) pasó de español a inglés. Los `.md` de esta carpeta se siguen escribiendo en español (son para el equipo interno), pero el copy citado dentro de cada uno ya refleja el inglés real de la UI.

## Convención de cada archivo

1. **Propósito** — qué resuelve esta sección en la experiencia
2. **Decisiones tomadas** — con fecha y el porqué, no solo el qué
3. **Estado actual de implementación** — qué está real (interactivo) vs. mock/placeholder
4. **Pendiente / abierto** — lo que se difirió a otra iteración, para no perderlo
5. **Archivos relacionados** — dónde vive el código

## Estado global del prototipo

- **Modo técnico:** HTML standalone multi-flujo (no hay `Prototype/components.json` en este repo)
- **Journey cubierto hoy:** Login → Home (sidebar + chat) → primer artefacto generado
- **No cubierto todavía:** contenido real de Agents (Apps ya tiene vista + ciclo de vida real, ver `home.md`/`apps.md`), creación de agente vía chat con lógica real, detalle de un proyecto individual (la vista de "todos los proyectos" ya existe — ver `home.md` → sección Projects)
- **Persistencia:** login real, esquema completo, lectura y escritura reales contra Supabase ya existen (Fases 0-2, ver `supabase.md`) — crear/archivar/pinear/editar/borrar Projects/Apps/Chats ya persiste. Mensajes de chat, Realtime, mapa del proyecto y RLS fino por rol siguen pendientes (Fases 3-6)
- **Login:** ver decisiones de propuesta de valor en el propio `index.html` (todavía no tiene `.md` dedicado porque es una sola pantalla sin mucha ambigüedad de estructura; si gana complejidad se documenta aparte)

## Historial de sesiones

| Fecha | Qué se agregó/cambió |
|---|---|
| 2026-08-12 | Primera versión: login split-screen, home Codex-style, sidebar completo, chat con split-pane de artefacto |
| 2026-08-12 | Assets de marca reales aplicados: `Simetrik_logo.svg` (wordmark) en el login, `Simetrik_isologo.png` (marca reducida) en el logomark del sidebar y en el avatar del agente dentro del chat. Ver `assets/img/` para el resto de recursos recibidos, pendientes de definición (ver nota abajo). |
| 2026-08-12 | Login reconstruido a partir de imagen de referencia: split light (login)/dark (propuesta de valor), mockup flotante de workflow, `Cover.png` como fondo del panel oscuro. Todo el prototipo (login, home, sidebar, chat) traducido a inglés. Naming del producto ajustado a "Simetrik V3" / "Simetrik as Code" siguiendo la referencia. |
| 2026-08-12 | Auditoría de centrado de contenedores en el login (checklist agregado a `login.md`): todos los flex containers icono+texto ya usaban `align-items: center` explícito. Confirmado que el logo queda justificado a la izquierda, encima de "Welcome to Simetrik V3". Pendiente: confirmación visual en navegador real (no se pudo capturar screenshot en este entorno). **Home todavía no se toca — se espera indicación explícita del usuario.** |
| 2026-08-12 | Cambios del home aplicados: fix real de centrado en `.msg` y `.typing-indicator` (avatar se deformaba en mensajes de 2 líneas por falta de `align-items`). Confirmado que logomark del sidebar y naming "Simetrik V3" en el `<title>` ya estaban correctos, sin cambios necesarios ahí. |
| 2026-08-12 | Centrado del login ajustado: panel izquierdo centrado en los dos ejes (logo se separó a `position: absolute` top-left para no interferir), panel derecho centrado solo verticalmente (simétrico) manteniendo el texto justificado a la izquierda. |
| 2026-08-12 | Revisión del mismo día: logo del login vuelve a estar en flujo (ya no `position: absolute`), directamente encima de "Welcome to Simetrik V3", más grande (24px → 36px). Panel derecho ahora también centrado en ambos ejes (antes solo vertical). Favicon agregado (`Simetrik_isologo.png`), por ahora solo en `index.html`. |
| 2026-08-12 | Fix: `.mockup-wrap` ("Workflow · Period close") se había angostado como efecto secundario del centrado del panel derecho (pasó de `stretch` a `shrink-to-fit` sin querer). Se le dio `width: 560px` explícito para que vuelva a su ancho original. |
| 2026-08-12 | `simetrik-agent-icon.png` confirmado como ícono oficial del agente (no era un archivo suelto). Reemplaza al isologo genérico en el headline "Everything goes through the agent." del login, agrandado de 30px a 36px. |
| 2026-08-12 | Campos de login prellenados para demo: email real (`ivan.roa@simetrik.com`), password ficticia (`demo1234`, no la real que se pidió originalmente — ver `login.md` para el porqué). |
| 2026-08-12 | Logo: +24px de separación con el título. Panel derecho: tratamiento de tarjeta flotante (margin 24px + border-radius 24px, flush a la izquierda). Mockup card: loop de animación (tipeo del prompt → pasos del pipeline se encienden en secuencia → badges aparecen como respuesta del agente → reset). Corrección: eyebrow/headline/subhead vuelven a estar justificados a la izquierda (pills y mockup card siguen centrados). Favicon confirmado sin cambios — coincide con `Simetrik_isologo.png` ya aplicado. |
| 2026-08-12 | Panel derecho del login: `max-width: 700px` agregado — deja de estirarse a lo ancho de la columna del grid en pantallas anchas. |
| 2026-08-12 | Revertido: se sacó el `max-width: 700px` del panel derecho, vuelve a ocupar toda su columna del grid. |
| 2026-08-12 | Panel derecho: padding horizontal 64px → 224px (izq/der). Animación del mockup: se agregó avatar de persona (ícono `user`) junto a la burbuja del chat, para que se sienta como alguien chateando de verdad. |
| 2026-08-13 | Projects: nav item "Projects" del sidebar ahora abre una vista propia (`#projectsView` en `flows/home/index.html`) en vez de no hacer nada — buscador + tag filters + toggle grid/list + cards/rows con status (Production/Draft) y avatar, adaptando el patrón visto en `mock-v3/flows2/home/index.html`. Reusa los 3 proyectos mock ya existentes en Pinned/composer/search. Cards sin acción real al click, por decisión explícita (mismo criterio que el resto de links sin destino del prototipo). Ver `home.md` → sección Projects. |
| 2026-08-13 | Panel lateral del chat (botón `panel-right`, esquina superior derecha del chat): implementado el contenido que faltaba. Al abrirse sin nada seleccionado, pregunta "What do you want to open?" listando Projects y Apps (Map se descarta como opción separada — es una interpretación de la vista de un proyecto, no una entidad propia). Cada selección abre un tab (ícono + nombre + botón de cerrar); un "+" junto a los tabs reabre el mismo picker para agregar otro. Contenido de cada tab, placeholder por ahora ("Project view — X" / "App view — Y"). Ver `chat.md` → sección "Panel lateral del chat". |
| 2026-08-13 | Nuevo `proyecto.md`: documento de concepto (no de pantalla) definiendo el modelo funcional de Proyecto — organización → Administrador de cuenta (único con capacidad de crear Proyectos) → Proyectos individuales, cada uno con su propia gestión de usuarios invitados (rol + acciones scoped a ese Proyecto, no globales). Deja explícito que el prototipo hoy no implementa nada de esto (los proyectos son mock plano) y anota preguntas abiertas, incluida la reconciliación de "Organización" con el término de glosario "Espacio de trabajo". |
| 2026-08-13 | Ajustes al panel lateral del chat (feedback directo sobre la primera pasada): (1) el open picker ahora queda centrado en los dos ejes del panel, título y listas incluidos, en vez de arrancar pegado arriba; (2) el botón "+" de agregar tab se re-parenta dentro de la propia lista de tabs en cada render, así queda siempre inmediatamente después del tab recién abierto y se corre solo al final cuando se abre uno nuevo, en vez de vivir fijo afuera de la fila con scroll; (3) el tab activo pasó de azul primario a gris neutro (mismo criterio que el toggle grid/list de Projects) para no competir con la navegación real del sidebar; (4) la barra de tabs ahora mide 50px fijos, igual que `.chat-thread-header`/`.sidebar-header`, para quedar alineada horizontalmente con el header del hilo de al lado. Ver `chat.md` → sección "Panel lateral del chat". |
| 2026-08-13 | Panel lateral del chat, segunda ronda de ajustes: (1) **Bug corregido** — el popover del "+" no se veía porque el ajuste anterior lo re-parentaba dentro de `#panelTabsList`, que tiene `overflow-x: auto` (esto computa `overflow-y` también como `auto` por spec, clippeando el popover); revertido a hermano fijo de la lista, que de por sí ya queda siempre después del último tab sin necesitar reordenarse. (2) El open picker ahora tiene buscador en vivo + tabs de filtro (All/Projects/Apps/**Agents**, categoría nueva) — las listas dejan de ser exhaustivas por diseño (si hubiera 100 proyectos no se listarían todos), el usuario acota escribiendo o por categoría. Ítems más anchos, mismo look que `.search-result-item` del modal Cmd+K. El mismo buscador + tabs se replicó también en el popover del "+" (300px), con estado de búsqueda/filtro independiente del picker de pantalla completa. Ver `chat.md` → sección "Panel lateral del chat". |
| 2026-08-13 | Panel lateral del chat, tercera ronda: `.panel-open-results`/popover pasaron de `max-height` a `height` fija — con `max-height`, menos resultados encogían la caja y como todo `.panel-open-inner` está centrado (`justify-content: center`), el bloque completo (título/buscador/tabs incluidos) se recentraba y "saltaba" en cada tecla o cambio de tab; ahora esa caja siempre mide lo mismo, solo su contenido interno hace scroll. **Bug adicional corregido**: al abrir el primer tab, el popover del "+" aparecía ya abierto (permanentemente visible, no solo al clickear) — `.composer-menu.panel-open-menu` fijaba `display: flex` con la misma especificidad que `.composer-menu.is-open { display: block }`, y por estar más abajo en la hoja ganaba siempre sin importar el estado real; movido a `.composer-menu.panel-open-menu.is-open` para que el `display: flex` solo aplique junto con el estado abierto real. Ver `chat.md` → sección "Panel lateral del chat". |
| 2026-08-13 | Panel lateral del chat, cuarta ronda: **bug de posición corregido** — el botón "+" quedaba varado en el extremo derecho de la barra de tabs, con un hueco vacío antes de él, en vez de pegado inmediatamente al último tab abierto. Causa: `#panelTabsList` tenía `flex: 1`, estirándose a ocupar toda la barra mientras los tabs (empaquetados a la izquierda) dejaban ese espacio vacío en el medio. Quitado el `flex: 1` (ahora shrink-to-fit) — el "+" queda siempre pegado al último tab, sea que haya uno, dos o más abiertos. Ver `chat.md` → sección "Panel lateral del chat". |
| 2026-08-13 | Panel lateral del chat, quinta ronda: alineación dinámica del popover del "+". Arranca en `align-start` (abre hacia la derecha del botón) porque al inicio el botón está cerca del comienzo de la barra de tabs con espacio de sobra; a medida que se agregan tabs y el "+" viaja hacia el final de la barra, `updatePanelOpenMenuAlign()` mide con `getBoundingClientRect()` en cada render si abrir hacia la derecha desbordaría el borde derecho de `#chatSidePanel` y, de ser así, cambia a `align-end` (abre hacia la izquierda del botón) — así el popover nunca queda pisado ni cortado por el borde del panel. Ver `chat.md` → sección "Panel lateral del chat". |
| 2026-08-13 | Sidebar: cada fila de `Pinned` gana un botón de elipsis (hover-reveal) con un popover de 4 acciones — Unpin project, Invite members, Archive project, Edit info — mismo look que el menú de acciones ya existente en las cards de la vista Projects, pero con clases propias (`.sidebar-project-actions-*`) para evitar la colisión de nombres ya sufrida antes con `.project-row`/`.search-tab`. Las 4 acciones son no-op por decisión explícita del usuario: solo abren/cierran el popover, sin lógica real todavía — "Edit info" en particular es una acción completamente nueva, sin definir aún qué campos del proyecto editaría. Requirió separar `.project-row` (el botón que expande/colapsa la fila) del nuevo botón de elipsis en markup hermano, no anidado, porque un `<button>` no puede contener otro `<button>`. Se agregó `data-project-id` a cada `<li class="project-item">` (mismos ids de `PROJECTS_DATA`) como preparación para cuando se wireen las acciones de verdad. Ver `sidebar.md` → sección "Ellipsis de acciones por proyecto en Pinned". |
| 2026-08-13 | Sidebar, ajuste sobre la misma pasada: **hover unificado** — el fondo de hover se movió de `.project-row` (solo cubría el nombre) a `.project-row-wrap` (fila completa, nombre + ellipsis), así el ellipsis lee como parte de la misma superficie en vez de flotar aparte. **Popover convertido en portal** — vivía anidado en la lista de Pinned, que está dentro de tres ancestros con overflow no-visible (scroll propio de `.sidebar-section` + `overflow:hidden` de `.sidebar-section-body`/`-body-inner` para la animación de colapso), así que se recortaba sin importar qué `position` usara. Se sacó del subárbol del sidebar — ahora vive como hermano de `.app-shell` cerca de `</body>` (mismo patrón que el modal de Search), matcheado a su trigger por id. Abre hacia la **derecha** del botón (no debajo), posicionado con `position: fixed` calculado vía `getBoundingClientRect()` en `initSidebarProjectActions()`, con fallback a la izquierda y clamp contra los bordes de la ventana; se cierra en scroll/resize para no quedar desalineado de su botón. Ver `sidebar.md` → sección "Ellipsis de acciones por proyecto en Pinned". |
| 2026-08-13 | Panel lateral del chat, sexta ronda: **bug corregido** — con suficientes tabs abiertos, el botón "+" viajaba tanto hacia la derecha que terminaba casi debajo del botón flotante "Hide panel" (`.chat-panel-toggle`, que flota fuera del layout de la barra de tabs, así que nada la detenía antes). Fix: `.panel-tabs-bar` ahora reserva `padding-right: 56px`, el mismo espacio que `.chat-thread-header` ya reserva para ese mismo botón — `#panelTabsList` se queda sin espacio antes de esa franja y empieza a hacer scroll horizontal interno de sus propios tabs, mientras el "+" se asienta en una posición fija que nunca vuelve a moverse ni pisa el botón de Hide panel. Ver `chat.md` → sección "Panel lateral del chat". |
| 2026-08-13 | **Nuevo `chat-side-panel.md`**: se extrajo toda la documentación del panel lateral del chat (que hasta ahora vivía como una sección larga dentro de `chat.md`) a un archivo propio, dado el volumen de comportamiento y bugs acumulados en esta sola pieza (open picker, buscador/filtros, tabs, posición del "+", alineación dinámica del popover, 5 bugs encontrados/corregidos). `chat.md` quedó con un resumen de 3 párrafos que apunta al archivo nuevo, siguiendo la convención "un archivo por sección" ya establecida. Aclaración explícita en el archivo nuevo: no es el mismo componente que `sidebar.md` (ese es el sidebar de navegación de la app) — es un panel local al chat. |
| 2026-08-13 | Panel lateral del chat, séptima ronda: scrollbar del scroll horizontal de tabs corregido en dos partes — (1) `#panelTabsList` pasó a `align-self: stretch` para llenar los 50px de alto de `.panel-tabs-bar` (que centra a sus hijos por defecto), así su scrollbar queda pegado al `border-bottom` de la barra en vez de flotar a mitad de camino; (2) se sobrescribió el scrollbar global de `tokens.css` (thumb siempre visible) para que el thumb sea transparente en reposo y solo aparezca (mismo color de siempre) al hacer hover sobre la fila de tabs. Ver `chat-side-panel.md` → sección "Comportamiento". |
| 2026-08-13 | **Nuevo token `--scrollbar-size: 4px`** en `shared/tokens.css` — el grosor del scrollbar (antes un `4px` hardcodeado directo en la regla `::-webkit-scrollbar`) pasó a variable, y se agregó `scrollbar-width: thin` global (equivalente más cercano en Firefox, que no soporta un valor en px). `.panel-tabs-list` del panel lateral del chat ahora referencia esa misma variable explícitamente (`::-webkit-scrollbar { height: var(--scrollbar-size); }`) en vez de depender solo de heredar la regla universal — pedido explícito del usuario para que el grosor quede fijo en un solo lugar y no varíe entre componentes en futuras implementaciones. |
| 2026-08-13 | Panel lateral del chat: nueva acción **"Expand" / "Restore width"** (`#chatPanelExpandToggle`), al lado de "Hide panel" dentro de un wrapper común (`.chat-panel-actions`). "Expand" colapsa la columna de conversación a 0 (el panel pasa a ocupar el 100% de `.chat-pane`) y además fuerza colapsado el sidebar de navegación de la app (`#sidebar`, reutilizando su propia clase `.is-collapsed` de `shell.js`) para un modo foco completo — recuerda si el sidebar ya estaba colapsado de antes para no des-colapsarlo de más al hacer "Restore width". Cerrar el panel entero, iniciar un chat nuevo, o navegar a Projects/Apps/Agents del nav también salen del modo foco automáticamente. `padding-right` de `.chat-thread-header`/`.panel-tabs-bar` subió de 56px a 92px para despejar los dos botones. Ver `chat-side-panel.md` → sección "Expand / Restore width". |
| 2026-08-13 | **Nuevo `chats-and-tasks.md`**: el nav item "Chats and Tasks" del sidebar deja de abrir el empty state genérico compartido con Apps/Agents (`sectionCopy['chats-tasks']`, eliminado) y pasa a abrir una vista real (`#chatsView`) — lista global de todos los chats de la cuenta, con o sin contexto (Project/App/Agent). Mismo patrón visual que la vista Projects (título + buscador) pero simplificado por instrucción explícita: sin Filter, sin toggle grid/list, solo filas. Nuevo `CHATS_DATA` consolida los 6 chats que ya existían repartidos entre el markup de Pinned/Recent y `SEARCH_DATA.chats`, ahora con un campo `context` real (reusa `PROJECTS_DATA`/`PANEL_APPS_DATA`/`PANEL_AGENTS_DATA`, las mismas fuentes que ya usan el selector de contexto y el panel lateral) — se agregaron 2 chats nuevos solo para tener un ejemplo real de contexto App y Agent. Botón "Select" habilita selección masiva (checkbox por fila, ellipsis oculta) con **Archive como única acción masiva** (explícitamente sin Delete ni mover a proyecto). Ellipsis por fila con una sola acción, "Rename chat", visible pero deliberadamente sin modal ni campo inline todavía. El concepto de "Task" sigue sin definir — por instrucción explícita, no hay ningún tratamiento visual de eso en esta pasada. Ver `chats-and-tasks.md`. |
| 2026-08-13 | Chats and Tasks, segunda pasada: **loading skeleton** agregado (mismo tratamiento que Projects — 700ms al entrar a la sección, 400ms en cada búsqueda con debounce de 300ms, nunca en Select/Archive), y **bug corregido**: el `padding: 32px 40px` de `.chats-view` no se estaba aplicando (la vista quedaba pegada arriba, sin el margen que sí tiene Projects) porque el comentario CSS de esa sección contenía sin querer la secuencia `*/` dentro del texto (`.chats-*/.chat-list-row*`), cerrando el comentario a mitad de frase y corrompiendo la regla real que venía después. Verificado con Chrome headless antes y después del fix (`getComputedStyle` mostraba `flex: 0 1 auto`/`padding: 0px` con el bug, `flex: 1 1 0%`/`padding: 32px 40px` ya corregido). Ver `chats-and-tasks.md` → sección "Bug encontrado y corregido". |
| 2026-08-13 | Chats and Tasks, tercera pasada: la ellipsis de cada chat gana **Pin chat / Unpin chat** (primera opción, antes de "Rename chat"), pedido explícito del usuario para que fijar un chat impacte directamente el Pinned del sidebar. Pinear crea dinámicamente una fila nueva dentro del mismo `<ul class="project-list">` donde ya viven los 3 proyectos pineados (mismo mecanismo que ya existía para que un proyecto recién creado se pinee sin markup estático previo, `ensureSidebarPinnedItem`, extendido ahora a chats vía `ensureSidebarPinnedChatItem`) — la fila del chat en el sidebar tiene su propia ellipsis (Unpin chat / Rename chat, portal popover) directamente ahí, resolviendo el segundo pedido explícito ("si está en Pinned, habilita las mismas acciones directamente en el sidebar"). Un solo `toggleChatPinned(id)` es el punto de verdad para ambas ellipsis. Clases nuevas (`.sidebar-pinned-chat-*`) en `shared/tokens.css`, nunca comparten nombre con `.project-row`/`.sidebar-project-actions-*`. Archivar un chat pineado (bulk Archive) también lo oculta del sidebar, mismo criterio que el Archive de Projects. Verificado end-to-end con Chrome headless: pin → aparece en el sidebar → unpin desde ahí → desaparece, para los 3 tipos de contexto (Project/App/Agent). Ver `chats-and-tasks.md` → sección "Pin / Unpin: impacta el Pinned del sidebar". |
| 2026-08-13 | Chats and Tasks, cuarta pasada: **"Rename chat" deja de ser no-op** — abre un modal real (`#renameChatModalBackdrop`/`.rename-chat-modal`), mismo tratamiento visual que el resto de los modales del prototipo (backdrop con blur, scale-in centrado, header/body/footer), pedido explícito del usuario. Un solo campo ("Chat name", reusa el primitivo genérico `.create-project-field`), pre-rellenado con foco + selección automática, "Save changes" deshabilitado solo si queda vacío. Una sola instancia compartida por las dos ellipsis (la de la vista y la del sidebar): `renameChat(id)` ahora llama a `openRenameChatModal(id)` en vez de solo cerrar el popover. Al guardar actualiza `chat.name` directamente en `CHATS_DATA` y, si el chat está pineado, empuja el nombre nuevo también a su fila en el sidebar (`findSidebarPinnedChatItem` matchea por id, no por nombre, así que no hace falta buscar la fila antes de sobreescribir como sí necesita el rename de Projects). Verificado end-to-end con Chrome headless: pinear → renombrar → la fila de la vista y la del sidebar muestran el nombre nuevo. Ver `chats-and-tasks.md` → sección "Rename chat: modal real". |
| 2026-08-14 | Sidebar: **bug corregido** — navegar a Projects/Chats and Tasks/Apps/Agents con el sidebar colapsado lo reabría solo, aunque la navegación en sí funcionaba bien. Causa: `onNavSectionChange` llama `setPanelExpanded(false)` en cada cambio de sección para resetear el panel lateral del chat, pero `sidebarWasCollapsedBeforeExpand` (la bandera que decide si esa función debe restaurar el sidebar) arranca en `false` y nunca se actualiza salvo que "Expand" del panel lateral se haya usado — así que esa llamada, aunque el panel nunca hubiera estado expandido, siempre terminaba sacando `.is-collapsed` del sidebar sin condición. Fix: `setPanelExpanded()` corta temprano si el estado pedido ya es el actual, así una llamada que no representa ningún cambio real no toca el sidebar. Ver `sidebar.md` → sección "Bug corregido (2026-08-14)". |
| 2026-08-14 | Sidebar colapsado: clickear el ícono de Pinned ahora abre un **popover a la derecha** con el listado completo de proyectos pineados + sus chats directamente debajo (sin paso de expandir cada uno) y los chats pineados sueltos, con buscador en vivo — antes ese click solo togglaba un `.is-expanded` invisible a 52px, sin ninguna forma real de ver Pinned sin expandir todo el sidebar. Portal (mismo patrón que el popover de acciones por proyecto, `.sidebar-project-actions-menu`), posicionado con `getBoundingClientRect()` — la Pinned list vive dentro de `.sidebar-section`, que recorta overflow para su propio scroll, así que cualquier popover anidado ahí se corta sin importar el `position` que use. Lee el DOM vivo de Pinned en cada apertura/tecla en vez de mantener una segunda lista de datos, para no repetir el bug de "dos mocks separados que divergen" ya visto antes en este prototipo. Seleccionar un ítem solo cierra el popover (sin destino real, mismo criterio que el modal de Search). Fix chico en `initSectionCollapse()` para que el toggle normal de expand/collapse de la sección no se siga disparando en paralelo mientras el sidebar está colapsado. Ver `sidebar.md` → sección "Popover de Pinned en sidebar colapsado". |
| 2026-08-14 | Popover de Pinned colapsado, ajuste sobre la misma pasada (feedback directo): el header de cada proyecto ya **no** muestra sus chats siempre desplegados — ahora abre/cierra igual que la fila real de Pinned, mismo crossfade folder↔folder-open y mismo hover unificado de toda la fila ("igual que el hover state de los elipsis"). Logrado reusando literalmente las clases reales del sidebar (`.project-item`/`.project-row-wrap`/`.project-row`/`.folder-icon`/`.chat-sublist`/`.chat-row`, y `.sidebar-pinned-chat-row-wrap` para chats pineados sueltos) en vez de clases propias del popover — el comportamiento sale idéntico gratis, sin riesgo de que las dos implementaciones diverjan. Clickear el nombre de un proyecto solo abre/cierra (nunca selecciona, igual que la fila real); clickear un chat sí selecciona y cierra el popover. El estado abierto/cerrado por proyecto se siembra desde el sidebar real al abrir el popover pero vive independiente desde ahí (no escribe de vuelta). Buscar fuerza los proyectos con resultados a mostrarse expandidos, sin necesitar un click extra. Ver `sidebar.md` → sección "Popover de Pinned en sidebar colapsado". |
| 2026-08-14 | Popover de Pinned colapsado, tercer ajuste (pedido explícito): altura pasa de fija (340px) a **dinámica según contenido**, con `min-height: 200px` / `max-height: 300px` — por debajo del máximo la caja crece/encoge con el contenido real; al superarlo, `#sidebarPinnedPopoverResults` habilita scroll interno (`overflow-y: auto` + `min-height: 0`, necesario para que el flex item pueda encogerse por debajo de su contenido y que el `max-height` del contenedor realmente frene el crecimiento). A diferencia del modal de Search y el open picker del panel lateral (altura fija a propósito, por contenido centrado — redimensionar ahí causaría un salto visual de recentrado), este popover está anclado arriba, así que el resize no genera ese mismo jank. Sí se agregó un reposicionamiento defensivo (`repositionPopover()`, llamado tras cada tecla del buscador y al expandir un proyecto) que sube el popover únicamente si su borde inferior quedaría fuera de la ventana tras crecer — nunca lo mueve hacia abajo ni recalcula el eje horizontal. Ver `sidebar.md` → sección "Popover de Pinned en sidebar colapsado". |
| 2026-08-13 | Chats and Tasks, quinta pasada, pedido explícito del usuario: **(1) rebautizado a "Chats"** — el nav item, su tooltip y el `<h1>` de la vista sacan la palabra "task" del copy visible mientras ese concepto siga sin definir (el nombre interno, `data-nav="chats-tasks"` y `chats-and-tasks.md`, se mantiene). **(2) Tabs All / Archived** debajo del header, mismo patrón que las tabs de ownership de Projects (`.projects-ownership-tab`) pero con clase propia (`.chats-view-tab`) y solo 2 opciones (acá no hay "owner"). `chatsTab` se combina con la búsqueda en `getFilteredChats()`; empty state dedicado ("No archived chats.") cuando Archived no tiene nada, mismo criterio que Projects. "Select" se oculta en la tab Archived (no tiene sentido archivar en bulk algo ya archivado) y sale del modo selección si estaba activo al cambiar de tab. La ellipsis de un chat archivado se recorta a solo "Rename chat" (sin Pin/Unpin) — mismo criterio que Projects ya aplica a sus propias cards archivadas, para no resucitar en silencio una fila del sidebar de algo que ya no aparece en All. La tab activa persiste entre visitas, igual que el filtro de ownership de Projects. Verificado end-to-end con Chrome headless: archivar → desaparece de All → aparece en Archived con ellipsis recortada → Select oculto ahí → volver a All lo sigue excluyendo. Ver `chats-and-tasks.md` → sección "Tabs All / Archived". |
| 2026-08-14 | **Auditoría de documentación** (pedido explícito del usuario: verificar que lo último implementado quedó reflejado en los docs). `chat-side-panel.md` tenía dos brechas reales entre código y texto, corregidas: (1) el bug fix de `setPanelExpanded` (guard de salida temprana para que un `setPanelExpanded(false)` defensivo en cada cambio de sección no re-expandiera el sidebar cuando el modo foco nunca se había activado) no estaba mencionado — agregado a la sección "Expand / Restore width". (2) El **resize handle** (`#chatResizeHandle`, arrastrar el límite entre conversación y panel entre 370px y `paneWidth - 280px`, con reset a 370px al cerrar el panel o iniciar chat nuevo) no tenía ninguna sección propia pese a ser parte central del componente — se agregó completa. También se sumó una nota sobre los íconos de historial/new-chat del header reutilizando la reserva de 92px ya documentada. El resto de los componentes tocados en la sesión (tokens de scrollbar, `PANEL_APPS_DATA`/`PANEL_AGENTS_DATA`, `.panel-filter-tab`/`.panel-open-item`) se verificaron presentes y consistentes con el código actual — sin cambios necesarios ahí. |
| 2026-08-14 | **Auditoría de documentación, del lado del sidebar** (mismo pedido, alcance propio): comparado `shared/shell.js`/`shared/tokens.css`/el markup del sidebar en `flows/home/index.html` línea por línea contra `sidebar.md`. 3 brechas reales encontradas y corregidas: (1) el header del doc decía "Última actualización: 2026-08-12", desactualizado frente a los cambios del 13 y 14 documentados más abajo en el mismo archivo — actualizado a 2026-08-14. (2) "Archivos relacionados" no mencionaba `initSidebarCollapsedPinnedPopover()` (la función del popover de Pinned colapsado) ni `.sidebar-pinned-popover*`/`.sidebar-pinned-chat-*` explícitamente, ni las funciones del lado de Projects/Chats que escriben en la lista de Pinned (`ensureSidebarPinnedItem`/`ensureSidebarPinnedChatItem`/`wireSidebarProjectActionsBehavior`/`wireDynamicSidebarActionsTrigger`) — completado. (3) El nav item se renombró de "Chats and Tasks" a "Chats" en el código (bien documentado en `chats-and-tasks.md` por el otro lado), pero `sidebar.md` seguía usando "Chats and Tasks" en 3 lugares donde describía el copy visible (el Invariante de collapse, el bug de `setPanelExpanded`, y la nota de fuente de verdad del popover de Pinned) — corregido a "Chats" en los tres, dejando el encabezado de sección "## Chats and Tasks" y la explicación del rename tal cual (son el identificador interno/histórico, no copy que el usuario vea). |
| 2026-08-14 | Sidebar: **New Chat gana atajo real ⌘N/Ctrl+N**, pedido explícito del usuario. El listener global de `keydown` (mismo bloque que ya manejaba `⌘K` para Search) hace `document.getElementById('newChatBtn').click()` con `preventDefault()` — reusa los handlers reales del botón (reset del chat, estado activo del nav) en vez de reimplementar nada, mismo patrón ya usado por "New Chat" dentro de la vista Chats. Indicador visual permanente (no un tooltip): un chip `<kbd class="nav-item-kbd">⌘N</kbd>` a la derecha del label, siempre visible en estado expandido — a diferencia de Search (ícono sin label, atajo solo visible al hover en su `.tooltip-bubble`), New Chat ya muestra su texto, así que un tooltip ahí sería redundante; el chip sigue el mismo criterio que los atajos `⌘1`/`⌘2` de ejemplo del modal de Search. En colapsado el chip se oculta (sin espacio a 36px) y el `data-tooltip` del botón pasa a `"New chat (⌘N)"` para que el atajo siga siendo descubrible ahí. Ver `sidebar.md` → sección "New Chat: atajo de teclado real". |
| 2026-08-14 | **Nuevo `apps.md`**: documento de concepto (no de pantalla, mismo formato que `proyecto.md`), escrito *antes* de construir nada de la vista Apps — pedido explícito del usuario. Descarta el framing viejo de "Apps = integraciones externas" (Slack/Snowflake/Google Drive, el mock actual de `PANEL_APPS_DATA`/`SEARCH_DATA.apps`) y define el modelo real, dado directamente por el usuario: una App es un **artefacto persistido, construido conversacionalmente**, que analiza proyectos, toma decisiones, genera informes/comunicados, y puede ingestar/accionar sobre datos — relación **muchos a muchos** con Proyectos (0..N, nunca contenida en uno solo), con varios chats asociados igual que un Proyecto. Documenta el ciclo de vida acordado: abrir una App existente replica `openProjectChat()` (chat angosto + panel auto-abierto con el artefacto renderizado); crear una App nueva usa el mismo mecanismo arrancando en blanco — el panel se construye en vivo durante la conversación inicial, y el agente deriva nombre + descripción de la App de ese intercambio (no un modal de formulario). Dejado explícito en "Pendiente/abierto": metadata final de card, reemplazo del mock viejo, copy del empty state actual, alcance de "accionar sobre datos", relación con Agents. Nada de esto está construido todavía — este documento es solo la definición conceptual previa, análoga a cómo `proyecto.md` precedió el trabajo real de invitaciones/roles. |
| 2026-08-14 | Sidebar, dos fixes sobre lo agregado el mismo día (feedback directo del usuario): **(1) el atajo de New Chat no funcionaba** — `⌘N`/`Ctrl+N` está reservado por Chrome/Safari/Firefox para "abrir ventana nueva", el `keydown` ni le llega a la página, así que ningún `preventDefault()` lo iba a interceptar (`⌘⇧N` tampoco sirve, Chrome lo usa para incógnito). Cambiado a **`⌥⌘N`/`Ctrl+Alt+N`**, no reclamado por ningún navegador mayor. **(2) El tooltip colapsado no se veía como un `<kbd>` real** — usaba el sistema simple de texto plano (`data-tooltip`), por eso se veía como texto entre paréntesis en vez del chip que sí muestra Search. `#newChatBtn` pasó al sistema rico (`.tooltip-bubble` con un `<kbd>` real adentro, mismo componente de Search), con una regla de supresión nueva para que no aparezca también en expandido (donde ya sería redundante con el label + el chip `⌥⌘N` inline) — necesaria porque, a diferencia de Search, New Chat es un solo botón presente en los dos estados, no dos elementos separados. Ver `sidebar.md` → sección "New Chat: atajo de teclado real". |
| 2026-08-14 | Sidebar: **bug corregido** — hacer scroll adentro del popover de Pinned (sidebar colapsado) lo cerraba solo. Causa: el listener que cierra el popover al scrollear la página usa fase de captura (`document.addEventListener('scroll', closePopover, true)`, necesario para oír el scroll de `.sidebar-section`), pero esa fase también recibe el scroll de cualquier descendiente — incluido el propio `#sidebarPinnedPopoverResults`, que tiene `overflow-y: auto` desde el fix de altura dinámica de la pasada anterior. Fix: el listener ahora chequea `popover.contains(e.target)` antes de cerrar, así un scroll que se origina adentro del popover ya no lo afecta. Ver `sidebar.md` → sección "Popover de Pinned en sidebar colapsado". |
| 2026-08-14 | Sidebar: **`⌥⌘N` tampoco funcionaba** — el usuario reportó que Chrome lo usa para dividir la vista (split view). Segundo cambio de atajo en el día: New Chat pasa a **`⇧⌥⌘N`/`Ctrl+Shift+Alt+N`** (3 modificadores + letra) — sin registro de que ningún navegador mayor ni utilidad común de ventanas lo reclame por defecto. Se agregó, además, el pedido de **`⌘B`/`Ctrl+B` para colapsar/expandir el sidebar** — a diferencia de `⌘N`, `⌘B` sí es interceptable (los navegadores solo lo tratan especial dentro de edición de texto enriquecido, no lo reservan a nivel de browser-chrome) y es una convención ya usada por VSCode/Notion/Linear/Slack para "toggle sidebar". Vive en `initSidebarCollapse()` (`shell.js`), llamando directo a la misma `toggleSidebar()` que ya usan los dos botones — sin duplicar lógica. Los tooltips de esos dos botones (`#sidebarToggle`/`#sidebarLogoExpand`) pasaron del sistema simple (`data-tooltip`) al rico (`.tooltip-bubble` con `<kbd>⌘B</kbd>` real), mismo pedido de "que se vea como un KBD, como Search" ya resuelto para New Chat — sin necesitar regla de supresión esta vez, porque cada botón existe en un solo estado (nunca los dos a la vez). Requirió sumar `.logo-mark.sidebar-logo-icon` a las reglas de hover de `.tooltip-bubble`, que antes solo cubrían `.icon-btn`/`.nav-item`. Ver `sidebar.md` → secciones "New Chat: atajo de teclado real" y "Sidebar collapse/expand: atajo de teclado real". |
| 2026-08-14 | **Nuevo `project-map.md`**: documento de concepto (no de pantalla, mismo formato que `proyecto.md`/`apps.md`) que cierra el modelo de nodos del Project View Map — Integración (con dirección entrada/salida), Dataset (sin variantes, la diferencia entre un dataset normal y uno resultado de conciliación vive en tags libres, nunca en el tipo) y Function; el Ruleset (las reglas/SQL que producen un Dataset) queda explícitamente fuera del mapa como nodo. Registra las 3 versiones descartadas antes de llegar acá — los 6 tipos vistosos de los mocks, luego 2 tipos + variante "conciliación" sacados del código real ya shipped (`grafo.ts`), corregido dos veces por el usuario hasta la versión final — para que la próxima fase de implementación (Fase 2 del mapa, sin construir todavía) no tenga que re-derivar ninguna de las dos correcciones. |
| 2026-08-14 | **Construida la sección Apps real**, sobre la definición conceptual de `apps.md` (fila de arriba). Nav item "Apps" ya no abre el empty state genérico — abre `#appsView`, catálogo con buscador, Filter by project (multi-select sobre los proyectos que al menos una App referencia), tabs All/Pinned/Archived, loading skeleton y ellipsis (Edit detail/Pin-unpin/Archive, o Edit detail/Delete si está archivada) — mismo patrón que Projects, propia familia de clases (`.apps-*`/`.app-*`, nunca comparte nombre con `.projects-*`/`.project-*`). **3 Apps propuestas y sembradas**, demostrando el rango 0/1/N de proyectos vinculados que pedía el modelo conceptual: `Bank Reconciliation Summary` (1 proyecto), `Quarterly Anomaly Digest` (2 proyectos), `Board Update Draft` (0 proyectos, `status: 'draft'`). **Abrir/crear una App generaliza exactamente el mecanismo ya existente para Project** (`openProjectChat`/`startNewProjectFlow`/`pendingNameFromChat`): `openAppChat`/`startNewAppFlow`/`renameAppFromFirstMessage`, con `startChat()` extendido para bifurcar por tipo en vez de asumir siempre Project. El tab `app` del panel lateral dejó de ser el placeholder de texto plano compartido con Agent — `renderAppArtifactTab` muestra un "not built yet" (mismo lenguaje visual que el empty state del Project Map) mientras la App sigue sin definir, y el artefacto real (ícono/nombre/status/proyectos de origen/cuerpo) una vez que el primer mensaje la define. **Diagnóstico y reemplazo del mock de integraciones en los 3 lugares donde aparecía** (pedido explícito del usuario): `PANEL_APPS_DATA` (Slack/Snowflake/Google Drive) pasó a ser `APPS_DATA` (las 3 Apps reales), alimentando sin cambios adicionales el picker del panel lateral, el selector de contexto del chat y las menciones `@` (ya genéricos vía `panelOpenDataFor`/`iconForItem`, este último generalizado para leer `.icon` de cualquier tipo, no solo Project); `SEARCH_DATA.apps` (modal Cmd+K) actualizado a los 3 nombres reales; el chat de ejemplo con contexto App en `CHATS_DATA` retargeteado de `google-drive` a `bank-reconciliation-summary`. Ver `home.md` → sección "Apps" y `apps.md` → "Estado actual de implementación" para el detalle completo, incluidos los gaps dejados explícitos (Pin/Unpin no sincroniza con el sidebar, "App chats" no tiene su propio ícono de historial en el header todavía). |
| 2026-08-14 | Sidebar: **`⇧⌥⌘N` tampoco funcionó** — cuarto intento del atajo de New Chat en el día. Pedido explícito del usuario tras este fallo: dejar de apilar modificadores, volver a algo simple (un solo Cmd/Ctrl + una letra). Cambiado a **`⌘J`/`Ctrl+J`** — razonamiento aplicado (sin poder verificarlo en vivo, este entorno sigue sin navegador conectado): los atajos de Chrome para la letra J son `⇧⌘J` (Downloads) y `⌥⌘J` (consola JS), ambos con modificador extra sobre Cmd; `⌘J` solo no aparece reservado en ningún lado, mismo patrón por el que `⌘B` (Chrome reserva `⇧⌘B` para la barra de marcadores, no `⌘B` solo) sí funcionó para el toggle del sidebar. Historial completo de los 4 intentos, con el motivo de cada fallo, documentado en `sidebar.md` → sección "New Chat: atajo de teclado real". |
| 2026-08-14 | Sidebar: **bug corregido** — los tooltips de "Open sidebar" y "New Chat" no se veían para nada al hacer hover. Causa: `.tooltip-bubble` es `position: absolute` y necesita un ancestro posicionado para anclarse — sin uno, usa el viewport completo como referencia y termina renderizado fuera de pantalla. El sistema simple (`[data-tooltip]`) trae ese `position: relative` de fábrica; al convertir estos dos botones al sistema rico (para los atajos `⌘B`/`⌘J`), perdieron ese anclaje porque ni `.nav-item` (New Chat) ni `.logo-mark` (Open sidebar) lo tenían — a diferencia de `.icon-btn` (Close sidebar, Search), que sí, por eso esos dos nunca se rompieron. Fix: `position: relative` agregado a ambas clases. Efecto secundario positivo: corrige también un bug preexistente nunca reportado — el tooltip de Search en el rail colapsado tenía exactamente el mismo problema desde antes de esta sesión. Ver `sidebar.md` → sección "Tooltips". |
| 2026-08-14 | Sidebar: **popover de Recent en sidebar colapsado**, pedido explícito del usuario, "similar al de Pinned". Mismo mecanismo exacto (`initSidebarCollapsedRecentPopover()` en `shell.js`, portal + `position: fixed` vía `getBoundingClientRect()`, buscador en vivo, `min-height: 200px`/`max-height: 300px` con scroll interno, reposicionamiento defensivo, fix de scroll-cierra-el-popover) pero más simple porque `#sidebarRecentList` ya es una lista plana — el popover solo renderiza `.chat-row` reales, sin la lógica de expand/collapse por proyecto que sí necesita Pinned. Todos los bugs ya encontrados en el popover de Pinned se aplicaron acá desde el arranque, no se repitieron. Clases propias (`.sidebar-recent-popover*`), copiadas 1:1 de `.sidebar-pinned-popover*`. El guard de `initSectionCollapse()` que evitaba el toggle silencioso de `.is-expanded` en Pinned mientras está colapsado se extendió a Recent; `toggleSidebar()` ahora cierra los dos popovers al expandir/colapsar. Ver `sidebar.md` → sección "Popover de Recent en sidebar colapsado". |
| 2026-08-14 | Sidebar: **ícono de chat (`message-circle-more`) agregado a cada fila de Recent**, pedido explícito del usuario ("en Recientes solo vamos a abrir Chat"). Nuevo modificador `.chat-row--icon` en `shared/tokens.css` — no un cambio a `.chat-row` en sí, que se sigue usando sin ícono en `.chat-sublist` de Pinned (esos chats ya se distinguen por estar indentados bajo un proyecto). Aplicado en los 3 lugares donde una fila de Recent se renderiza: la lista estática del sidebar, `registerNewChat` (arrancar un chat nuevo), y el popover de Recent en sidebar colapsado agregado en la pasada anterior — para que este último no quedara desactualizado frente a la fila real. De paso, se agregó `lucide.createIcons()` donde faltaba (`registerNewChat` y el `renderResults()` del popover de Recent nunca lo habían necesitado, al insertar solo texto antes). Ver `sidebar.md` → sección "Ícono de chat en cada fila de Recent". |
| 2026-08-14 | Panel lateral del chat, pedido explícito del usuario: **skeleton de carga** en el open picker ("What do you want to open?") y en el popover del "+", ya que Projects/Apps/Agents ahí son "en teoría, bajo petición". Mismo mecanismo ya establecido por Projects/Apps/Chats and Tasks (`skeletonShimmer` global + su gradient, clases propias `.panel-open-skeleton-*`): 700ms tipo "primera carga" al abrir/reabrir el picker (panel recién abierto, o al cerrar el último tab) y al abrir el popover del "+"; 400ms tipo "re-filtrado" al buscar o cambiar de tab de filtro, con un timer por scope que cancela cualquier fetch simulado anterior si llega uno nuevo antes. Guardado (`maybeLoadPanelOpenPicker`) para no dispararse con el panel ya cerrado. Ver `chat-side-panel.md` → sección "Open picker". |
| 2026-08-14 | Search (modal Cmd+K), pedido explícito del usuario: **loading skeleton** para los resultados — hasta ahora cada "consulta" (abrir el modal, tipear, cambiar de tab) mostraba el resultado final de inmediato, sin ningún estado de carga. Mismo mecanismo ya establecido por Projects/Apps/Chats and Tasks y el open picker del panel lateral (`skeletonShimmer` global + su gradient): 700ms tipo "primera carga" al abrir el modal, 400ms tipo "re-filtrado" al tipear o cambiar de tab, un timer (`searchQueryTimer`) que cancela cualquier fetch simulado anterior si llega uno nuevo antes. Clase propia `.search-skeleton-item` (no comparte `.search-result-item`) — necesario porque Search tiene navegación real por teclado (↑/↓/Enter) y click-to-seleccionar sobre esa clase; compartirla habría hecho una fila shimmer resaltable/seleccionable mientras todavía es placeholder. Plano (no agrupado por categoría como el resultado real en "All"), mismo criterio de simplificación que ya usa el esqueleto del open picker del panel lateral. Ver `search.md` → sección "Loading skeleton". |
| 2026-08-18 | **Fase 0 de persistencia real (Supabase)**: proyecto Supabase nuevo y separado de `mock-v3`, creado vía dashboard (login CLI no viable en este entorno, sin TTY). 4 migraciones (`supabase/migrations/0001-0004`) aplicadas a mano en el SQL Editor: `organizations`/`organization_members` con roles (multi-org desde el día 1), `projects`/`apps`/`apps_projects`/`agents` con seed de la demo actual, `chats` (a lo sumo un contexto: project/app/agent, nunca varios) + `chat_messages` (tabla nueva, no existía backing store), `map_nodes`/`map_edges`/`map_versions` (versionado con snapshot completo, sin seed — el mapa arranca vacío). `shared/supabase-config.js`/`shared/auth.js` nuevos (mismo patrón que `mock-v3`). Login real en `index.html` (`signInWithPassword`/`signInWithOAuth('google')`, ya no hay credenciales de demo prellenadas) y guard de sesión + logout real en `flows/home/index.html`. Los `_DATA` arrays de `flows/home/index.html` **siguen en memoria** — leer/escribir contra Supabase de verdad es Fase 1/2, todavía sin hacer. Ver `supabase.md`. |
| 2026-08-18 | **Fase 1 de persistencia real (lectura)**: `PROJECTS_DATA`/`APPS_DATA`/`CHATS_DATA`/`PANEL_AGENTS_DATA` en `flows/home/index.html` dejan de ser `const [...]` hardcodeados y pasan a `let []` poblados por `bootstrapDataFromSupabase()` (4 funciones `load*FromSupabase()` nuevas, fire-and-forget al cargar la página) — mismo shape camelCase que ya consumía todo el código de filtrado/render existente, cero cambios ahí. `owner` de Project ahora se deriva de `owner_user_id` comparado contra la sesión activa (ya no es un string hardcodeado); `chat.createdAt` ahora es tiempo relativo real calculado desde `created_at`. Efectos secundarios conocidos y documentados en `supabase.md`: los 3 proyectos salen con `owner: 'shared'` (el seed no fijó `owner_user_id`), y hay un breve flash de estado vacío en Projects/Apps al cargar la página hasta que la carga real resuelve (ambas vistas se renderizan síncrono al inicio, no solo al navegar). **Todavía sin escritura real** (Fase 2) — crear/archivar/pinear sigue sin persistir, se pierde en cada reload. Ver `supabase.md`. |
| 2026-08-18 | **Fase 2 de persistencia real (escritura)**: crear/renombrar/pinear/archivar/borrar/editar en Projects, Apps y Chats ya persiste de verdad contra Supabase, en vez de solo mutar el array en memoria. `createProject`/`createApp`/`registerNewChat` y ~13 funciones más pasaron a `async` con su `insert`/`update`/`delete` real; los ids de lo nuevo ahora los asigna Supabase (`uuid` real vía `insert().select().single()`), no el slug que generaba el cliente (`generateProjectId`/`generateAppId`/`generateChatId` quedaron sin uso). Se investigó primero cada cadena de llamadas para confirmar que convertir estas funciones a `async` no cascadeaba — casi ningún caller necesitaba esperar el resultado, así que la conversión fue de bajo riesgo. `apps_projects` (N:N) se actualiza por diff (agrega/borra solo lo que cambió) tanto al crear como al editar una App. Patrón de error: muta en memoria y re-renderiza primero, dispara la escritura después, sin rollback ni aviso visible si falla (solo `console.error`) — aceptable en prototipo. Ver `supabase.md`. |
| 2026-08-18 | Search (modal Cmd+K), pedido explícito del usuario: tabs de filtro `All`/`Chats`/`Projects`/`Apps`/`Agents` pasan de pill/badge (fondo redondeado, tinte azul en la activa) a **underline tabs** (texto plano + borde inferior de 2px en la activa, con `--color-ink`, no un negro literal). Truco de `margin-bottom: -1px` para que el borde de la tab activa se solape exactamente sobre el separador que ya tenía `.search-modal-tabs`, en vez de flotar con un hueco. Cambio acotado a `.search-tab` únicamente — varios componentes (`.projects-ownership-tab`, `.panel-filter-tab`, `.chats-view-tab`, `.apps-view-tab`) habían copiado 1:1 el estilo pill viejo en clases propias (para evitar el wiring global de `.search-tab` que ya rompió tabs ajenas antes, ver `home.md`), así que no heredan este cambio automáticamente y quedaron con el look pill sin tocar. Ver `search.md` → sección "Tabs de filtro: pill → underline". |
| 2026-08-18 | Search, mismo pedido en la misma sesión: **removidos los dos `border-bottom`** que separaban input→tabs y tabs→resultados (de más, ahora que las tabs son underline y ya aportan su propia señal visual) — el truco `margin-bottom: -1px` de la fila anterior se revirtió junto con esto, ya no tenía contra qué alinearse. **Footer de atajos agregado**, tono gris (`--color-surface-sunken`): `↑` `↓` **Move** y `↵` **Select**, calcado del pie de un command-palette típico (Linear/Raycast/GitHub) — es la única línea divisoria que le queda al modal (`border-top` del footer). `<kbd>` en `--color-surface` (blanco, no el mismo gris del footer, para que no queden invisibles) con glifos de texto (`↑`/`↓`/`↵`), no un ícono embebido — mismo patrón que ya usan `⌘K`/`⌘J`/`⌘B` en el resto del archivo. Labels en mayúscula vía CSS (`text-transform: uppercase`), no escritos en mayúscula en el HTML. Ver `search.md` → sección "Dividers removidos + footer de atajos". |
| 2026-08-18 | Search, tercera ronda de ajustes sobre el footer/resultados (feedback directo): **(1)** hint `↵` agregado a cada fila de resultado, visible solo en hover/highlight por teclado (mismo trigger que ya tenía el tinte de fondo de la fila) — puro CSS, sin JS nuevo. **Reemplaza** a los `⌘1`/`⌘2` decorativos de dos ítems de ejemplo (`July period close`/`Nequi source differences`), que se sacaron junto con la clase `.search-result-kbd` que ya no se usa en ningún lado. **(2)** Labels del footer ("Move"/"Select") corregidos de mayúscula forzada (`text-transform: uppercase`) a sentence-case — el markup ya estaba bien escrito, sobraba la transformación. Ver `search.md` → secciones "Labels: sentence-case" y "Hint de Enter al hacer hover/highlight". |
| 2026-08-18 | Search, cuarta ronda (pedido explícito, con 2 decisiones confirmadas antes de implementar): **tags de Projects visibles en el resultado + filtro real por tag**. `SEARCH_DATA.projects` (lista mock estática, desacoplada a propósito) se eliminó — nueva `getSearchItems(cat)` lee `'projects'` en vivo de `PROJECTS_DATA` (con `tags`, excluyendo archivados), el resto de categorías sigue en `SEARCH_DATA` sin cambios. Tags como texto plano después del nombre ("Name - tag - tag", no pills coloreados), anidadas dentro de `.search-result-name` para compartir su mismo truncamiento. Botón nuevo "Filter by tag" (arriba a la derecha de la fila de búsqueda) — mismo patrón exacto que ya usan Projects/Apps (label/valor + chevron, popover multi-select, OR entre tags activas, combinable con texto y tab), copiado 1:1 en clases propias (`.search-filter-*`). Riesgo conocido sin resolver: el popover vive dentro de `.search-modal` (`height:500px` + `overflow:hidden`) — con el pool actual (5 tags reales del seed) entra bien, pero un catálogo de tags mucho más grande podría recortarse contra el borde del modal, mismo tipo de bug ya resuelto antes en los popovers del sidebar. Ver `search.md` → sección "Tags de Projects: mostradas en el resultado + filtro por tag". |
| 2026-08-18 | Search, quinta ronda de refinamiento (feedback directo): **(1)** gris de las tags aclarado — `--color-ink-faint` + `opacity: .7` encima (no hay token de tinta más claro en `shared/tokens.css`, mismo idioma que ya usa `.folder-icon` para "mismo color, más claro"). **(2)** Botón "Filter by tag" pasó de texto completo a **ícono solo** (`settings-2`, reusando `.icon-btn` directamente — primitivo genérico real, sin wiring atado a su clase) con tooltip, más un **dot azul** (`.dot`, también genérico) en la esquina superior derecha cuando hay tags activas — reemplaza al texto "all"/tag/"N tags" de la versión anterior. **(3)** Buscador nuevo adentro del popover (`#searchFilterSearchInput`) para poder "buscar y seleccionar uno o varios" en listas de tags más largas — filtro instantáneo local, sin skeleton/delay. "Clear filters" se mantuvo sin cambios, pedido explícito. Ver `search.md` → sección "Tags de Projects: mostradas en el resultado + filtro por tag". |
| 2026-08-18 | Search, sexta ronda de ajustes chicos (feedback directo): **(1)** "Clear filters" tenía demasiado padding respecto a su alto — solo llevaba la clase base `.composer-menu-option` (pensada para opciones de dos líneas), a diferencia de las filas de tags de arriba (`.composer-menu-option--model`, compacta). Se le agregó el mismo override que ya tiene `#projectsFilterClear` en Projects (`min-height:32px; padding:0 8px; align-items:center`), que se había pasado por alto al copiar el patrón. **(2)** El hint `↵` dejó de mostrarse en mouse hover — es un affordance de navegación por teclado ("solo en los asociados directamente al teclado con las flechas"), un click ya selecciona directo; ahora solo reacciona a `.is-highlighted`. **(3)** El glifo `↵` se veía descentrado horizontalmente (métrica propia del carácter U+21B5 en la tipografía, no un bug de centrado flex) — envuelto en su propio `<span class="kbd-glyph-enter">` con `transform: translateX(1px)`, aplicado tanto en el hint por fila como en el `↵` del footer. Ajuste visual sin verificar en navegador real (este entorno sigue sin uno conectado). Ver `search.md` → secciones "Hint de Enter..." y "Tags de Projects...". |

| 2026-08-18 | **Project View Map, Fase 2: los nodos, implementados directo en `flows/home/index.html`** (no solo en el mockup "Nodos del Mapa" de la sesión anterior) — pedido explícito: "vamos trabajando directamente en el mapa dentro del proyecto". Card de 2 capas (header gris + card blanca interior, ver `map.md`) para los 3 tipos (Integración con dirección, Dataset, Function), 2 estados (completo/colapsado con tooltip), handles parejos, aristas dirigidas (punteada para la que alimenta una Function), tags libres y "Last updated" en los 3 tipos. Nuevo token `--color-accent`/`--color-accent-tint`/`--color-accent-ink` (violeta, solo para Function) en `shared/tokens.css`. Las aristas se calculan con `offsetLeft/Top/Width/Height` de cada nodo (no `getBoundingClientRect()` como en el mockup) porque ahora sí conviven con el `transform: scale()` del pan/zoom de Fase 1 — así no hace falta recalcularlas al panear/hacer zoom, solo cuando un nodo cambia de tamaño. El grafo se centra en vivo contra el tamaño real del canvas (no un valor fijo que solo calzaría por casualidad). **Divergencia deliberada y temporal**: como `PROJECTS_DATA` ahora sale de Supabase (ver fila de abajo) con ids que no se pueden anticipar, el grafo de ejemplo (`DEMO_MAP_GRAPH`, escenario "LATAM Bank Reconciliation") se muestra en cualquier proyecto por ahora, no en uno scopeado — se cierra cuando el primer mensaje del chat dispare el reveal real. **Nuevo `map.md`**: se extrajo de `home.md` (que quedó con un resumen de 2 líneas) toda la documentación de Fase 0/Fase 1/Fase 2 del mapa, mismo criterio que ya separó `chat-side-panel.md` de `chat.md` — pedido explícito del usuario ("documentación... con todas las definiciones que vamos a realizar de comportamientos"). Sin verificar en navegador real, mismo motivo de siempre. |
| 2026-08-18 | **Migración a Supabase, Fase 0 y Fase 1** (fuera del alcance de este índice hasta ahora — documentado en detalle en `docs/supabase.md`, nuevo): proyecto Supabase real creado, separado del de `mock-v3`; migraciones iniciales (`organizations`, `organization_members`, `projects`/`apps`/`agents`, `chats`, y el esquema del Project Map — `map_nodes`/`map_edges`/`map_versions`, éste último ya con el modelo de 3 tipos de nodo del prototipo, no los 2 del producto real); login real (`signInWithPassword`/`signInWithOAuth`) reemplazando el fake anterior; `PROJECTS_DATA`/`APPS_DATA`/`CHATS_DATA`/`PANEL_AGENTS_DATA` pasan de arrays hardcodeados a poblarse vía `select()` contra Supabase al cargar. Se menciona acá porque cambia una asunción que varias filas de este historial daban por sentada (`PROJECTS_DATA` como array estático) — ver `map.md` → "El grafo de demo" para el efecto concreto que tuvo sobre el Project View Map. |
| 2026-08-18 | Search, séptima ronda (feedback directo): **(1)** "Clear filters" ahora arranca `disabled` y solo se habilita con 1+ tags activas — `updateSearchFilterButton()` (ya se llamaba en cada cambio de `searchActiveTags`) le suma `clearBtn.disabled = searchActiveTags.size === 0`. Nuevo estado genérico `.composer-menu-option[disabled]` en `shared/tokens.css` (opacidad + `cursor:not-allowed`, mismo idioma que `.btn-primary[disabled]`), disponible para cualquier otro `.composer-menu-option` que lo necesite después. **(2)** El glifo `↵` seguía viéndose descentrado tras el nudge de la ronda anterior — sin navegador para confirmar dirección/magnitud, se dejó de ajustar a ciegas y se reemplazó el carácter por el ícono real de Lucide `corner-down-left` (SVG, centrado geométrico garantizado) en los dos lugares donde se usaba (hint por fila + footer) — excepción puntual a "los kbd de este archivo son siempre texto, nunca un ícono", solo porque el texto no se pudo arreglar de forma confiable sin verificación en vivo. Ver `search.md` → secciones "Tags de Projects..." y "Hint de Enter...". |
| 2026-08-18 | Search, octava ronda (feedback directo): **subtítulos de categoría con menos grosor pero más peso visual** — `.search-group-title` (los labels "Chats"/"Projects"/"Apps"/"Agents" que agrupan resultados en la tab "All") pasó de `font-weight: 700` (bold) a `font-weight: 600` (semibold) + `letter-spacing: .04em` (antes `.02em`) — el "peso visual" que antes salía de la negrita ahora sale del tracking extra, no de más grosor. Color probado más oscuro (`--color-ink-soft`) en el mismo intento y revertido a `--color-ink-faint` (gris claro) a los pocos minutos, pedido explícito. Ver `search.md` → sección "Subtítulos de categoría: menos grosor, más peso visual". |

## Nota sobre assets pendientes (actualizada 2026-08-12)

En `assets/img/` hay más recursos de los que ya se aplicaron, todavía sin instrucción clara de uso:

- `Simetrik_logo_white.svg` / `Simetrik_logo_white_.svg` — variante blanca del wordmark, sin uso definido todavía (ningún panel del prototipo es de fondo sólido de color por ahora)
- ~~`simetrik-agent-icon.png` / `descarga.png` — mismo archivo duplicado~~ **Resuelto:** confirmado como el ícono oficial del agente (distinto del isologo de la marca). Ya aplicado en el headline del login. `descarga.png` (el duplicado) sigue sin usarse.
- `agent-icon.svg`, `onboarding-intro.mp4`, `user_profile.jpeg` — sin instrucción de uso todavía; `user_profile.jpeg` podría reemplazar las iniciales "IR" del avatar del footer si se confirma
- `Cover.png` — fondo degradado azul/negro, propósito sin confirmar
- `claude-logo_svgstack_com_...svg` — **es el isotipo de Claude/Anthropic (marca de terceros). Por instrucción explícita del usuario, NO se usa en el prototipo por el momento.** Queda en la carpeta sin referenciar en ningún HTML.
- Pendiente de recibir: tipografía "Belong" (sin archivo todavía) y recurso de referencia para el responsive del sidebar (sin archivo todavía)
