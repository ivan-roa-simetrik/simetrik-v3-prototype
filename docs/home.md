# Home

> Última actualización: 2026-08-12
> Archivo relacionado: `flows/home/index.html`

> **Nota de idioma (2026-08-12):** el copy de este flujo pasó de español a inglés (instrucción explícita del usuario). Las citas de copy más abajo son de la versión anterior en español; la estructura y las decisiones siguen vigentes, pero para el texto exacto actual ver `flows/home/index.html` directamente.

## Propósito

Es la pantalla a la que se llega después del login. Tipo Codex: layout de dos columnas, sidebar a la izquierda (navegación + Proyectos + Agents + Apps) y el chat como eje central a la derecha. Desde acá el usuario arranca cualquier cosa — crear un proyecto, generar un artefacto, diseñar un agente — conversando, no llenando formularios.

## Decisiones tomadas

- **Layout de dos columnas fijo, sin dashboard de métricas.** El home no es un tablero de KPIs ni una grilla de accesos directos. Es sidebar + chat. Motivo: el chat es el eje central del producto (Ley de identidad "plataforma agentizada, no caja de herramientas"); un dashboard tradicional hubiera competido por atención con la conversación.

- **Estado inicial = greeting/empty state, no dashboard poblado.** Al entrar, el chat arranca vacío con "¿Qué querés construir hoy?" + input + 3 suggestion chips, no con una lista de actividad reciente en el centro. Motivo explícito del usuario: el objetivo del prototipo es *vivir la experiencia completa* desde cero, no simular un usuario recurrente. El sidebar sí trae mock data (3 proyectos, chats recientes) para que no se sienta un producto sin uso, pero el centro de atención (el chat) arranca limpio.

- **El artefacto se muestra en split-pane, nunca reemplaza ni convive en tabs con el chat.** Cuando el chat genera un artefacto, el chat se achica a ~42% del ancho y el artefacto ocupa el resto. Se puede seguir escribiendo mientras se ve el artefacto. Alternativas descartadas: fullscreen (cortaba el eje conversacional) y tabs (menos fiel a "construir algo en paralelo a la charla"). Ver detalle de trade-offs en `chat.md`.

- **"Proyectos", "Apps" y "Agents" son navegación de primer nivel, con contenido específico diferido.** El usuario pidió que estas 3 entradas existan en el sidebar con comportamiento propio ("secciones específicas que desarrollamos más adelante"). Para no dejar clicks muertos, "Apps" y "Agents" muestran un empty state genérico explicando qué van a hacer; "Proyectos" no tiene vista propia todavía porque ya está representado en la lista del sidebar (ver `sidebar.md`).

## Estado actual de implementación

- ✅ Login → home funcional (navegación real vía `window.location`)
- ✅ Estado vacío del chat con suggestion chips funcionando
- ✅ Transición a hilo de chat + generación de artefacto mock (canned, no real)
- ✅ Split-pane funcional con animación de entrada (320ms ease-out)
- ✅ Empty states de Apps/Agents al clickear el nav correspondiente
- ⛔ Sin persistencia: recargar la página vuelve todo al estado inicial
- ⛔ Sin vista de "todos los proyectos" (grid/lista completa)
- ⛔ Sin lógica real de creación de proyecto — es simulación conversacional

## Pendiente / abierto

- Definir qué pasa en el chat cuando el usuario pide explícitamente "crear un agente" — ¿el artefacto pane se convierte en un formulario de configuración de agente, o es un flujo separado? Todavía no se ha explorado.
- Responsive/mobile no se evaluó — el layout de dos columnas fijas asume desktop.
- Projects: "New project" y el click en una card siguen siendo no-op — falta definir si el prototipo necesita simular la creación real de un proyecto o abrir un detalle al hacer click (ver sección Projects más abajo).

## Projects (2026-08-13)

`onNavSectionChange` ya no excluye `section === 'projects'` — el nav item abre una vista propia (`#projectsView`) en vez de no hacer nada, resolviendo el pendiente que existía acá arriba.

- **Patrón adoptado, con referencia concreta.** Se tomó como base la sección Projects de `mock-v3/flows2/home/index.html` (buscador + toggle grid/list + tag filters + cards/rows con estado y avatar) y se adaptó a los tokens y datos de este prototipo — no es una reimplementación 1:1, no hay componentes desyk reales ni backend detrás.
- **Reusa los 3 proyectos mock existentes** (`LATAM Bank Reconciliation`, `Q2 Journal Entry Audit`, `Q3 Treasury Forecast`) — mismos nombres que ya vivían en Pinned/composer/search, para no fragmentar el mock data del prototipo.
- **Fidelidad completa en metadata**: cada proyecto tiene tags (`source:...`, `region:...`, `period:...`) y un badge de estado (`Production`/`Draft`, clases `.badge--success`/`.badge--neutral` ya existentes en `shared/tokens.css`) — decisión explícita del usuario de no simplificar a solo nombre + ícono.
- **Buscador y tags filtran de verdad** (por nombre y por tag activo, combinables); grid/list es un toggle real, no decorativo.
- **Las cards/rows no navegan a ningún lado — decisión explícita.** A diferencia de la referencia (que deshabilita visualmente las cards sin detalle real detrás con `opacity-50`), acá se optó por **no** aplicar ningún tratamiento deshabilitado: mismo criterio que los chats de Pinned/Recent en el sidebar y los resultados del Search modal, que tampoco navegan a nada y tampoco se ven "rotos". El click no dispara ninguna acción.
- **"New project"** sigue siendo un botón visual sin acción, mismo no-op que el "+" de Projects en el sidebar y la opción "New project" del selector de proyecto en el composer.

### Header consolidado a 1 fila (2026-08-13, misma sesión)

Pasada de ajuste pedida por el usuario: el header original ocupaba 3 filas (título+botón, buscador+toggle, chips de tags). Se consolidó a **una sola fila** para no gastar tanto espacio vertical antes de mostrar un solo proyecto:

- **Buscador colapsado a ícono, al lado de Filter.** El input de texto ya no está siempre visible ni vive junto al título — es un botón `search` con el mismo tratamiento outline que Filter (`.projects-search-btn`: borde + fondo `--color-surface`, 32×32), ubicado inmediatamente a la izquierda de Filter. Al clickearlo "despliega" un campo inline que **crece hacia la izquierda** (el input vive antes del botón en el DOM/flex, así que al crecer ocupa el espacio vacío entre el título y los controles de la derecha, en vez de empujar Filter/toggle/New project — el grupo derecho ya está anclado al borde por el `justify-content: space-between` del header).
- **Botón e input nunca conviven — corrección 2026-08-13.** La primera versión mostraba el botón Y el input abierto al mismo tiempo; se corrigió a un solo control que "muta" entre dos estados: colapsado = solo el botón outline; abierto = el botón se oculta (`hidden`) y el input ocupa su lugar, con su propio ícono de lupa (leading, dentro del campo) y una X (trailing) que es la única forma de cerrarlo — cerrar limpia el término, vuelve a mostrar el botón, y re-filtra. Escape en el input también cierra.
- **"Filter" sin ícono, formato label/value + chevron.** Ya no es un botón con ícono de embudo — ahora lee como un selector compacto: "Filter by tag" en gris tenue (`--color-ink-faint`) + " : " + el valor activo en negro (`--color-ink`, ej. "all" sin selección, el tag mismo si hay 1, "N tags" si hay varios) + un chevron que rota 180° al abrir (vía `:has()` sobre el `.composer-menu.is-open` hermano — sin JS extra para mantenerlo sincronizado si el popover se cierra por click afuera). El popover interno no cambió: multi-select reusando `.composer-dropdown`/`.composer-menu`, cada tag alterna independiente (no exclusivo), el menú no se cierra al seleccionar, "Clear filters" al final.
- **Todo en una sola fila**: título a la izquierda; buscador + Filter + toggle grid/list + "New project" a la derecha (`.projects-header-left` / `.projects-header-right`, `justify-content: space-between`).
- **Altura uniforme de 32px** en todos los controles del header (búsqueda, Filter, toggle grid/list, "New project") **y en los ítems internos del popover de Filter** (cada tag + "Clear filters", vía `min-height: 32px` en `#projectsFilterList .composer-menu-option`/`#projectsFilterClear`) — pedido explícito del usuario para que quede alineado con inputs y botones del resto del prototipo.
- **Idioma: copy siempre en inglés.** "Filter by tag" / "all" / placeholder "Search projects" — el usuario aclaró explícitamente que da instrucciones en español pero el copy implementado debe ser siempre en inglés (corrige una pasada anterior de esta misma sesión que había dejado ese texto literal en español).
- **Tooltips del header abajo y centrados.** Search, Grid view y List view usaban la variante default de `[data-tooltip]` (aparece al costado derecho, heredada de `tokens.css`) — se les agregó `.tooltip-bottom` (misma clase que ya usan Search/Close sidebar del header del sidebar) para que aparezcan debajo, centrados horizontalmente bajo el ícono.
- **Toggle grid/list: activo en gris, no en azul primario.** `.project-view-toggle-btn.is-active` pasó de `--color-primary-tint`/`--color-primary-ink` a `--color-surface-sunken`/`--color-ink` — cambiar entre grid y list es una preferencia de vista de bajo impacto, no debería competir visualmente con acciones primarias (mismo criterio ya aplicado al ícono "Show/hide panel" del chat, que tampoco se pinta de azul).

### Cards/rows: hover sin fondo + menú de acciones (2026-08-13, misma sesión)

- **Hover sin color de fondo, en grid y list.** La card (`.project-card`) ya no tenía tinte de fondo en hover — solo faltaba corregir la fila de list view (`.project-list-row`, ver nota de rename abajo), que sí tenía `background: var(--color-surface-sunken)` en `:hover`. Ambas quedan iguales: el feedback de hover es visual pero sin relleno de color (shadow/border en la card; nada adicional en la fila) — el ícono de acciones apareciendo es el otro componente del affordance.
- **Elipsis vertical (`more-vertical`) en la esquina superior derecha de la card** (extremo derecho de la fila en list) — oculto por defecto, aparece con el hover de la card/fila o mientras su popover está abierto. Abre un popover con dos acciones, reusando `.composer-menu-option` para verse igual que el popover de Filter:
  - **Pin / Unpin project** — label e ícono (`pin`/`pin-off`) cambian según `project.pinned`. Es la única acción de este popover con estado cruzado real: al hacer toggle, busca el `<li class="project-item">` correspondiente en el sidebar por el texto de `.project-name` (no hay un data source compartido entre la vista Projects y el sidebar, son dos mocks separados) y le hace `hidden = true/false`. Los 3 proyectos arrancan `pinned: true` porque así están hoy en el sidebar (no existe un bucket de "proyectos sin pinear" en ningún otro lado del prototipo).
  - **Archive project** — saca el proyecto de esta vista (`project.archived = true`, filtrado en `getFilteredProjects()`) con una micro-animación de fade+scale-down (160ms) antes de re-renderizar, y también lo oculta del sidebar (mismo mecanismo que Unpin). **No hay una sección "Archived" para verlo después** — fuera de alcance de esta pasada, el proyecto simplemente deja de listarse en cualquier lado.
  - **Wiring vía delegación de eventos**, no vía el mecanismo `data-menu-trigger` del composer: las cards/filas se re-renderizan en cada cambio de filtro, así que un listener por-elemento se perdería en cada render. `wireProjectActionsDelegation()` engancha un solo `click` en `#projectsGrid`/`#projectsList` y resuelve con `closest()`.
  - **Fuera de alcance explícito**: pin/unpin/archive NO tocan el picker de proyecto del composer ni los datos mock del Search modal (`SEARCH_DATA.projects`) — esos siguen siendo listas estáticas separadas. Solo se sincroniza con el sidebar, que es lo que pidió el usuario explícitamente.
- **Bug encontrado y corregido en el mismo pase: colisión de nombre de clase.** La fila de list view usaba `.project-row`, el mismo nombre de clase que el sidebar ya usaba (en `shared/tokens.css`) para sus propios botones de proyecto en Pinned. Al compartir nombre, mis reglas nuevas (padding 12px 16px, hover, etc.) pisaban silenciosamente el padding/hover real del sidebar (7px 8px, con su propio hover gris intencional) — cascada: `tokens.css` se carga antes que el `<style>` inline de `flows/home/index.html`, misma especificidad, gana la última regla. Renombrado a `.project-list-row` (+ `-icon`/`-name`/`-tags`/`-actions-*`) en todo el archivo, sin tocar los 3 `<button class="project-row">` reales del sidebar. Lección: revisar nombres de clase contra `shared/tokens.css` antes de introducir una clase nueva con un nombre genérico.

### Footer de la card + formas cuadradas + tabs de ownership (2026-08-13, misma sesión)

- **Footer de la card separado por línea horizontal.** `.project-card-footer` (status + avatar) ganó `border-top: 1px solid var(--color-border)` — antes solo tenía `margin-top`, sin separación visual real de la fila de tags. Se lee como una sección de metadata distinta, no como una continuación del mismo bloque. Cambio scopeado a la card (grid); la fila de list ya mostraba status/avatar inline en una sola línea, no necesita este tratamiento.
- **Status tag cuadrado, no pill.** Nuevo modificador `.badge--square` en `shared/tokens.css` (`border-radius: var(--radius-sm)`, en vez del `999px` default de `.badge`) — se agregó como modificador aparte, no cambiando `.badge` global, porque es un primitivo genérico que podría reusarse después con su forma pill default. `statusBadge()` ahora emite `badge--success badge--square` / `badge--neutral badge--square`.
- **Avatar cuadrado, no circular.** `.project-avatar` pasó de `border-radius: 50%` a `border-radius: var(--radius-sm)` — alinea con el tratamiento que el avatar del footer del sidebar ya usa (ver `sidebar.md`: "cuadrado, no circular" es la convención de marca para avatares en este prototipo, no una excepción puntual).
- **Footer edge-to-edge + altura fija 40px (ajuste posterior, misma sesión).** El divisor no llegaba a los bordes de la card (quedaba dentro del padding de 16px). Primer intento: `margin: 14px -16px 0; padding: 0 16px` directo en `.project-card-footer` (margen negativo cancela el padding de `.project-card`, padding equivalente vuelve a alinear el contenido) — el usuario reportó que en la práctica el padding de la card se veía "sumado" en vez de cancelado, contenido ancho y descentrado.
- **Segundo intento: línea divisora desacoplada del contenido vía `::before`.** En vez de que `.project-card-footer` mismo cargue con margen negativo + padding compensatorio, el footer quedaba con el inset normal de la card y **solo la línea** (`::before`, `position: absolute; left: -16px; right: -16px`) llegaba a los bordes. El usuario volvió a reportar el mismo síntoma ("el bottom padding de la card hace que se vea muy ancho el footer") sin que hubiera forma de confirmar visualmente qué pasaba en este entorno (sin navegador real disponible).
- **Simplificado a la versión sin trucos (misma sesión, tercera pasada): sin margin negativo, sin `::before`.** Después de 2 rondas fallidas intentando que el divisor llegue al borde exterior de la card (bleed más allá del padding), se optó por la opción robusta: `.project-card-footer` vuelve a tener el inset normal de la card (mismo ancho que nombre/tags de arriba, sin `margin`/`padding` especiales), y el `border-top` es simplemente tan ancho como el footer mismo — **no** llega al borde exterior de la card, pero sigue leyéndose como una línea divisoria de una sección de footer. Se sacrificó el "de lado a lado" literal (tocar el borde exterior) a cambio de eliminar cualquier interacción rara con el padding de la card, que es lo que estaba generando el problema reportado dos veces seguidas.
- **Causa raíz real, encontrada por el usuario (misma sesión, cuarta pasada): no era la línea, era el padding-bottom de la card.** El "se ve ancho/abultado" de las dos rondas anteriores no era sobre el divisor en sí — era el espacio extra que dejaba el `padding: 16px` completo de `.project-card` *debajo* del footer. Fix con instrucción explícita y precisa del usuario: `.project-card` pasó a `padding: 16px 16px 4px` (shorthand de 3 valores: top 16px, left/right 16px sin tocar, bottom reducido a 4px — nada más cambia). Con esa causa resuelta, **el `::before` del segundo intento vuelve** para el divisor (`position: absolute; left: -16px; right: -16px` sobre `.project-card-footer`, que ahora sí es `position: relative`) — esta vez sí toca los bordes izquierdo y derecho de la card de lado a lado, como se pidió desde el principio. Altura fija de 40px se mantiene.

### Invite members en el popover de acciones (2026-08-13, misma sesión)

Nueva opción en el elipsis de cada card/fila, entre Pin/Unpin y Archive: **Invite members**. Conecta directamente con el modelo conceptual de `docs/proyecto.md` — "el acceso a un Proyecto es por invitación directa, proyecto por proyecto" — aplicado por primera vez a un flujo real de la UI, no solo documentado.

- **Segunda vista dentro del mismo popover, no un popover nuevo.** `.project-actions-menu` ahora tiene dos bloques internos: `.project-actions-menu-default` (las 3 opciones) y `.project-invite-view` (formulario), alternados vía `hidden` — evita tener que trackear un segundo elemento posicionado en absoluto. El popover se ensancha de 170px a 240px mientras se muestra la vista de invitar (`.is-inviting`).
- **Formulario mínimo: solo email, sin selector de rol.** Input de email + botón "Send invite" + un texto de ayuda ("They'll only get access to this project, not the rest of the organization.") que refuerza el mismo concepto de acceso scoped de `proyecto.md`. No se agregó selector de rol porque el catálogo de roles todavía es una pregunta abierta en ese documento — agregar uno ahora habría significado inventar roles que probablemente no coincidan con lo que se defina después.
- **"Send invite" es no-op, como el resto de las acciones de esta vista** — no sale ningún email real. Deshabilita el input y el botón, cambia el texto a "Invite sent" (sin destruir el markup del formulario, solo estado) y cierra el popover completo a los 900ms — mismo espíritu que las respuestas canned del chat.
- **Botón "Send invite" arranca deshabilitado**, se habilita solo cuando el input tiene texto — validación mínima, sin backend real detrás.
- **Bug encontrado y corregido en el mismo pase: click dentro del popover cerraba todo.** Al agregar el input de email, cualquier click ahí (para enfocarlo) no calzaba con ninguno de los `data-*` manejados explícitamente, así que burbujeaba hasta el listener global `document.addEventListener('click', () => closeAllProjectActionMenus())` y cerraba el popover antes de poder escribir. Fix: cualquier click cuyo `target` esté dentro de `.project-actions-menu` hace `stopPropagation()` incondicionalmente, antes de evaluar las acciones específicas.
- **Fuera de alcance explícito**: no hay lista real de miembros ya invitados, no hay validación de formato de email, no hay selector de rol, y la invitación no se sincroniza con ningún lado (a diferencia de Pin/Unpin y Archive, que sí tocan el sidebar).

### Loading skeleton (2026-08-13, misma sesión)

Pedido explícito del usuario: que entrar a Projects (y, en una segunda instrucción sobre la marcha, también buscar/filtrar por tag/cambiar de vista) primero muestre una animación de esqueleto de carga antes de renderizar las cards reales — simula que cada una de esas acciones "pide" datos, no solo la primera entrada a la sección.

- **Shimmer, no pulse.** Cada "hueso" del esqueleto (ícono, línea de texto, pill de tag, badge, avatar) tiene un gradiente de 3 tonos (`--color-surface-sunken` → `--color-border` → `--color-surface-sunken`) que anima su `background-position` en loop de 1.4s — una franja de brillo que recorre cada bloque, no un simple fade de opacidad. Duración intencionalmente fuera del canon de 120/200/320ms: ese canon es para microinteracciones puntuales, esto es un estado ambiental de carga.
- **Dos containers de skeleton** (`#projectsSkeletonGrid` con 3 cards, `#projectsSkeletonList` con 3 filas), cada uno copiando la estructura/espaciado real de `.project-card`/`.project-list-row` (mismo padding, mismo `border-top` de 40px en el footer del skeleton de card) para que el swap-in no salte de tamaño. Se muestra el que corresponde al `projectsViewMode` activo.
- **`queryProjectsWithLoading(delay)`**: función única que centraliza el ciclo completo — oculta grid/list/empty reales, muestra el skeleton correspondiente, espera `delay` ms, oculta el skeleton y llama a `applyProjectsFilters()`. Un solo timer (`projectsQueryTimer`) para que una query nueva cancele la anterior si todavía estaba "en vuelo" (ej. cambiar de tag filter justo después de buscar).
- **Dos duraciones**: 700ms para `loadProjectsView()` (entrar a la sección, sensación de "primera carga") y 400ms para cualquier re-filtrado (búsqueda, tags, ownership tabs, toggle grid/list) — mismo criterio que ya usaba la referencia original (`mock-v3`) para su propio skeleton, solo que acá se aplica a cada re-query, no únicamente al primer fetch.
- **Búsqueda con debounce de 300ms**: el término se actualiza en cada tecla (no se pierde lo que se escribe), pero el skeleton + re-render solo se disparan 300ms después de que el usuario deja de escribir — un flash de skeleton por cada tecla se hubiera visto roto, no como "cargando".
- **Fuera de alcance a propósito**: Pin/Unpin y Archive (en el popover de acciones de cada card) NO disparan el skeleton — no son "búsqueda/filtro/cambio de vista" (lo único que se pidió explícitamente), y Archive ya tiene su propia micro-animación de fade-out que hubiera competido visualmente con un flash de skeleton superpuesto.
- **Extensión no pedida explícitamente, aplicada por consistencia**: las tabs de ownership (All/Created by you/Shared projects) también disparan el skeleton, aunque el usuario solo mencionó "búsqueda, tags y cambio de vista" — se trataron como la misma categoría de "filtro que re-consulta la lista". Avisar si se prefiere revertir solo esa parte.
- **Bug encontrado y corregido (misma sesión, pasada posterior): salto de tamaño entre skeleton y card real.** El usuario reportó que la card renderizada tenía un tamaño distinto (más grande) que su versión en skeleton, generando un salto visual confuso al terminar la carga. Causa: aunque `.project-skeleton-card` copiaba el mismo padding/margin que `.project-card`, ninguna de las dos tenía una altura mínima explícita — la real varía un poco según cuántos tags tenga el proyecto (o si esos tags wrappean a una segunda línea dentro de una card angosta), y el skeleton, al no tener ese mismo contenido variable, terminaba más bajo. Fix: `min-height: 124px` agregado a **ambas** reglas (`.project-card` y `.project-skeleton-card`), con un comentario cruzado en el CSS advirtiendo que deben cambiar juntas si se ajusta el valor. De paso, se uniformaron las 3 cards del skeleton a 2 tag-pills cada una (antes la del medio tenía solo 1), para reducir variación entre ellas mismas.
- **Tabs de ownership: All / Created by you / Shared projects**, debajo del header, arriba del grid/list. Visualmente son el mismo componente pill que las tabs del Search modal — a diferencia del toggle grid/list (que se puso gris para bajar peso visual), estas tabs mantienen el tinte azul primario activo porque es un filtro de contenido real, mismo tipo de interacción que las tabs de categoría del Search modal, no una preferencia de bajo impacto.
- **Bug encontrado y corregido (misma sesión, pasada posterior): las tabs de ownership no filtraban.** Se habían implementado reusando literalmente la clase `.search-tab` para heredar el estilo — pero el modal de Search (Cmd+K) engancha un wiring global `document.querySelectorAll('.search-tab').forEach(tab => ...)` que también agarraba estos 3 botones (mismo nombre de clase). Cada click disparaba DOS handlers: el mío (correcto) y el del modal de Search, que leía `tab.dataset.tab` (`undefined` en mis botones, que usan `data-ownership`) y sobreescribía `.is-active` en TODOS los `.search-tab` del documento según `dataset.tab === undefined` — rompiendo el estado visual y, de paso, potencialmente el propio estado interno del Search modal (`searchActiveTab` quedaba en `undefined`). Mismo patrón de bug que la colisión de `.project-row` de antes, y el mismo que el otro agente concurrente ya había evitado a propósito para `.panel-filter-tab` (ver comentario en el CSS de esa clase). Fix: nueva clase standalone `.projects-ownership-tab` con las reglas de `.search-tab` copiadas 1:1, sin compartir el nombre. Lección repetida: cualquier clase nueva de "tab"/"chip" debe revisarse contra wiring global existente (`grep` por el nombre de la clase) antes de reusarla, no solo contra `shared/tokens.css`.
  - Nuevo campo mock `owner: 'you' | 'shared'` en `PROJECTS_DATA` — split 2/1 (LATAM Bank Reconciliation y Q2 Journal Entry Audit = `'you'`; Q3 Treasury Forecast = `'shared'`) elegido solo para que ambos filtros devuelvan resultados no vacíos en la demo, no representa ninguna regla de negocio real.
  - Filtro combinable con búsqueda y tags — los tres criterios (`projectsOwnershipFilter`, `projectsSearchTerm`, `projectsActiveTags`) se aplican en cadena dentro de `getFilteredProjects()`.
- Explícitamente fuera de esta pasada: el panel lateral que se abre desde el header del chat (`#chatSidePanel`, agregado en una iteración previa) — quedó pausado sin tocar, su contenido sigue sin definirse.

### Edit detail: nueva acción en el elipsis + modal placeholder (2026-08-13)

Pedido explícito del usuario: agregar una cuarta opción, **"Edit detail"**, al popover de acciones de cada card/fila (antes solo tenía Pin/Unpin, Invite members, Archive) — va primera en la lista, arriba de Pin/Unpin. Por ahora es deliberadamente un placeholder: el usuario aclaró que el contenido real del modal se define en una pasada posterior, esta solo necesita dejar el punto de entrada funcionando y el modal visible.

- **Nuevo modal propio, no una tercera vista dentro del popover de acciones.** A diferencia de "Invite members" (que reutiliza el mismo popover con una vista alternativa vía `hidden`), "Edit detail" abre un modal centrado independiente (`#projectEditModalBackdrop` / `.project-edit-modal`) — mismo tratamiento visual de backdrop + scale-in que el modal de Search (`.search-modal-backdrop`), pero su propia familia de clases porque este modal va a cargar un formulario real más adelante y no tiene sentido forzarlo dentro del popover angosto de 170-240px de la ellipsis.
- **Body 100% placeholder**: `.project-edit-modal-body` solo muestra "Coming soon — this modal's content is still being defined." — sin campos, sin formulario. `openProjectEditModal(project)` sí setea el título (`Edit ${project.name}`) para que ya se sienta contextual al proyecto correcto, aunque el cuerpo todavía no use esos datos.
- **Apertura/cierre**: click en "Edit detail" (cierra el popover de acciones primero) → abre el modal. Cierra con el botón "x" del header, click en el backdrop, o Escape — mismo mecanismo que `openSearchModal`/`closeSearchModal`.
- **Wiring**: nuevo `data-edit-detail="${p.id}"` en `projectActionsMenuMarkup(p)`, resuelto en `wireProjectActionsDelegation()` junto a los demás data-* de esa misma delegación de eventos.
- **Pendiente explícito**: el contenido del modal (qué campos, qué se puede editar) se define en una siguiente instrucción del usuario — no inventar campos todavía.

### Edit detail: formulario real, funciona en todos los entry points (2026-08-14)

Resuelve el pendiente de arriba. El body dejó de ser un placeholder — ahora tiene los mismos dos campos que "Create project" (mismo wording en pregunta, reusa las clases `.create-project-field*` en vez de duplicar estilos): **"What are you working on?"** (nombre, requerido) y **"What's the goal of this project?"** (descripción, opcional). Footer nuevo: "Cancel" / "Save changes" (`.btn-primary`, deshabilitado si el nombre queda vacío). Enter en el campo de nombre también guarda, igual que en el modal de creación.

- **Funciona en todos los puntos donde existe "Edit detail" sin tocar cada uno por separado** — porque todos ya llamaban a la misma `openProjectEditModal(project)` (cards/filas de esta vista, elipsis del sidebar en Pinned, tanto las 3 filas estáticas como cualquiera creada por `ensureSidebarPinnedItem`): construir el formulario real una sola vez, acá, alcanzó para que edite en cualquier lado desde donde se abra. No hizo falta wiring adicional en el sidebar ni en `shell.js`.
- **`saveProjectEdit()`** muta `project.name`/`project.description` directamente en `PROJECTS_DATA` y llama `applyProjectsFilters()` — como el resto de esta vista, no hay guardado "real", es el mismo mock in-memory de siempre.
- **Bug evitado, no encontrado en producción: renombrar un proyecto ya pineado podía desincronizar el sidebar.** `findSidebarPinnedItem()`/`toggleProjectPinned()` matchean la fila de Pinned por **nombre** — si se cambia `project.name` sin tocar el `<li>` ya existente, la próxima búsqueda por nombre viejo no encuentra nada (la fila queda "huérfana", invisible para pin/archive futuros). Fix: `saveProjectEdit()` busca la fila por el nombre **viejo** antes de sobreescribir `project.name`, y si la encuentra, actualiza su `.project-name` al nombre nuevo en el mismo momento.
- **No incluye el ícono** — ese picker vive solo en "Create project"; editar el ícono de un proyecto ya creado no se pidió y no se agregó acá.
- **Gap conocido, no resuelto**: si un proyecto está seleccionado como contexto de un chat activo (barra gris / header del hilo) y se le cambia el nombre desde acá, ese breadcrumb ya renderizado no se actualiza solo — son snapshots tomados al seleccionar, no bindings vivos a `PROJECTS_DATA`. Igual que otros puntos similares ya documentados en `chat.md`.

### Unificado con el "Edit info" del sidebar (2026-08-13, misma sesión)

El sidebar (trabajado por otra persona en paralelo) había agregado su propio elipsis en cada fila de `Pinned` con una acción placeholder equivalente, **"Edit info"** (ver `docs/sidebar.md` → "Ellipsis de acciones por proyecto en Pinned"). Decisión explícita del usuario: ambas deben abrir **el mismo modal**, y las acciones de Projects quedan administradas desde este lado — el sidebar solo es responsable de su propia interacción (abrir/cerrar/posicionar su popover), no de qué pasa al clickear cada opción.

- Renombrado el label del sidebar de "Edit info" a **"Edit detail"** (mismo nombre que esta vista) — misma acción, mismo modal (`openProjectEditModal`), dos entry points.
- Nueva función `wireSidebarProjectActionsBehavior()` (vive acá, junto a `toggleProjectPinned`/`archiveProject`/`openProjectEditModal`, no en `shell.js`) escucha clicks directamente sobre cada `.sidebar-project-actions-menu` del sidebar y despacha a las mismas funciones que ya usan las cards de esta vista: Unpin → `toggleProjectPinned(id)`, Archive → `archiveProject(id)`, Edit detail → `openProjectEditModal(findProjectById(id))`. `shell.js` sigue sin tocar — su único trabajo sigue siendo abrir/cerrar/posicionar el popover.
- "Invite members" del sidebar queda **sin wiring todavía** — ese popover no tiene la vista de formulario inline que sí tiene el de esta vista; ver `sidebar.md` → Pendiente.

## Create project (2026-08-13)

Flujo real de creación de proyectos — a diferencia de "Edit detail", este ya impacta datos: agrega una entrada real a `PROJECTS_DATA`. Administrado íntegramente desde esta vista (mismo criterio que "Edit detail"), con **3 entry points** que abren el mismo modal:

1. **Botón "New project" de la vista Projects** (`#newProjectViewBtn`) — ya existía, solo estaba sin wiring.
2. **El "+" del sidebar sobre el nav item "Projects"** (hover-reveal, `.nav-item-add`, ver `sidebar.md`) — `shell.js` ya ignoraba explícitamente estos clicks (`initNavActive()` los excluye de disparar cambio de sección) en anticipación a esto; se agregó el wiring real acá, no en `shell.js`.
3. **"New project" dentro del grupo Projects del selector de contexto del chat** (la barra gris, ver `chat.md` → "Selector de contexto") — único entry point con un comportamiento adicional: al crear, el proyecto nuevo queda **seleccionado por defecto** en esa misma barra (vía el callback `onCreate` de `openCreateProjectModal`).

### El modal

- **Título**: "Create project". Copy en inglés siempre, aunque la instrucción del usuario llegó en español ("Crear proyecto") — mismo criterio de idioma que rige todo el prototipo desde el 2026-08-12.
- **Dos campos, wording en forma de pregunta amigable** (pedido explícito): "What are you working on?" (nombre, input de texto, requerido) y "What's the goal of this project?" (descripción, textarea, opcional) — en vez de labels neutros tipo "Name"/"Description", para que el formulario mismo guíe qué información se espera, no solo qué campo llenar.
- **CTAs**: "Create project" (`.btn-primary`, deshabilitado hasta que el nombre tenga texto) y "Cancel" (`.btn-secondary`). Enter en el campo de nombre también dispara el submit si está habilitado.
- Mismo tratamiento visual (backdrop + scale-in) que `.project-edit-modal-backdrop`/`.search-modal-backdrop`, familia de clases propia (`.create-project-modal-*`) porque este sí carga un formulario real.
- **Backdrop con blur (2026-08-13, pedido explícito).** `backdrop-filter: blur(6px)` (+ `-webkit-backdrop-filter` para Safari) sobre el mismo `rgba(23, 23, 31, .4)` que ya tenía — el fondo detrás del modal ya no se ve 100% nítido. Aplicado también a `.project-edit-modal-backdrop` (mismo dueño, mismo patrón) para no dejar un modal con blur y el otro sin él dentro de esta misma vista. **No** se tocó `.search-modal-backdrop` (Cmd+K) — no es de esta vista, se deja consistente con lo que ya tenía a menos que se pida explícitamente extenderlo ahí también.
- **Título subido a 20px (2026-08-13, pedido explícito).** `.create-project-modal-title` pasó de 15px a 20px. Quedó desalineado a propósito de `.project-edit-modal-title` (se quedó en 15px) en ese momento — cambio pedido puntual para este modal, no una pasada de consistencia entre los dos.
- **Estandarizado entre modales (2026-08-14, pedido explícito de seguimiento).** El usuario pidió volver a un tamaño estándar de título entre los modales — `.project-edit-modal-title` subió a 20px también, y ambas reglas se agruparon en un solo selector (`.project-edit-modal-title, .create-project-modal-title { font-size: 20px; ... }`) en vez de mantener dos declaraciones idénticas por separado, para que un futuro cambio de tamaño no pueda volver a desalinearlas por accidente.

### Icon picker en "What are you working on?" (2026-08-13)

Pedido explícito del usuario, con referencia visual (screenshot del picker de ícono+color de Notion): el campo de nombre gana un botón de ícono embebido a la izquierda (folder por default, igual que hoy) que abre un selector para asociarle **cualquier ícono de la librería** al proyecto — **sin selector de color**, se descartó esa mitad de la referencia por instrucción explícita ("No incluir temas de colores").

- **Botón embebido dentro del input, no al lado.** `.create-project-icon-trigger` vive `position: absolute` dentro de `.create-project-name-row`, superpuesto al padding-left del input (36px) — mismo tratamiento que la referencia (el ícono se ve "adentro" del campo, no como un control separado).
- **Popover como portal**, mismo motivo y técnica que el popover de acciones del sidebar (`sidebar.md`): `.create-project-modal` tiene `overflow: hidden`, así que un popover anidado ahí quedaría recortado en vez de flotar por fuera de los bordes del modal, que es lo que necesita un grid de íconos con buscador + alto fijo. Se sacó a `#createProjectIconPopover`, hermano de `.app-shell`, posicionado `position: fixed` calculado con `getBoundingClientRect()` del trigger al abrir (debajo, con fallback hacia arriba y clamp contra los bordes de la ventana — mismo patrón que `initSidebarProjectActions()`).
- **"La librería completa" es Lucide, no una lista aparte.** En vez de mantener a mano una segunda copia de "todos los nombres de íconos" (inevitablemente se desincroniza), `getAllLucideIconNames()` lee directamente `window.lucide.icons` (el mismo paquete que ya usa todo `data-lucide` del prototipo) y convierte cada nombre de PascalCase a kebab-case (`pascalToKebab`) — así la búsqueda cubre de verdad el set completo que Lucide expone, sin hardcodear un catálogo separado que se pueda desincronizar. Si `window.lucide.icons` no estuviera disponible por algún motivo, cae a la lista curada de abajo en vez de romperse.
- **Vista default = 48 íconos curados, no las 1000+ de golpe.** `CREATE_PROJECT_ICON_DEFAULTS` es una lista fija (folder, dollar-sign, book, graduation-cap, briefcase, globe, flask-conical, paw-print...) que se muestra mientras el buscador está vacío — mismo principio ya aplicado en otros pickers del prototipo (el open picker del panel lateral, el selector de contexto del chat): "las listas no están pensadas para ser exhaustivas por defecto, el usuario acota escribiendo". Al escribir algo, el filtro pasa a correr sobre el set completo de Lucide, no solo sobre esos 48.
- **Selección aplica al toque, "Done" solo cierra.** Clickear un ícono en el grid actualiza el ícono del trigger inmediatamente (`setCreateProjectIcon`) y re-renderiza el grid para mover el resaltado (`is-selected`) — el popover se queda abierto, igual que en la referencia. "Done" (o click afuera, o Escape) simplemente cierra el popover; no hay un paso de "confirmar" separado de clickear el ícono.
- **Reset al abrir el modal**: cada vez que se abre "Create project" (desde cualquiera de los 3 entry points), el ícono vuelve a `folder` y el popover queda cerrado — no arrastra la última selección de una sesión anterior del modal.
- **El ícono elegido viaja con el proyecto, no solo se ve en el modal.** Se guarda como `project.icon` en `PROJECTS_DATA` y **reemplaza el `folder` hardcodeado** en cada lugar que renderiza un proyecto individual: cards/filas de esta vista (`renderProjectsGrid`/`renderProjectsList`), resultados y trigger del selector de contexto del chat, tabs y resultados del panel lateral, resultados de menciones `@`, y el breadcrumb del header del hilo de chat. Nueva función `iconForItem(type, item)` centraliza esto — sin ella, cada uno de esos 6 lugares seguía usando el ícono genérico por tipo (`PANEL_OPEN_ICONS`), que no sabe nada de un ícono custom por proyecto. Los 3 proyectos mock originales no tienen `icon` seteado, así que siguen mostrando `folder` vía el fallback (`p.icon || 'folder'` / `iconForItem` devuelve el default si no hay `item.icon`).

### Qué pasa al crear (`createProject()`)

- Genera un `id` slugificado desde el nombre (`generateProjectId`), con desambiguación `-2`/`-3`... si colisiona.
- Nuevo proyecto: `status: 'draft'` (nada pasó todavía ahí), `tags: []`, `pinned: false`, `archived: false`, `owner: 'you'` (el creador), `icon` (el elegido en el picker, `folder` si no se tocó).
- `PROJECTS_DATA.push(project)` + `applyProjectsFilters()` — como `PROJECTS_DATA` es la fuente única que también leen el selector de contexto del chat y el picker del panel lateral (`panelOpenDataFor('project')`), el proyecto nuevo aparece automáticamente en los tres sin wiring adicional.
- La `description` capturada se guarda en el objeto pero **todavía no se muestra en ningún lado** (ni card, ni row, ni ningún modal) — solo persiste en el dato. Mostrarla es una iteración futura.

### Bug corregido: pinear un proyecto nuevo no hacía nada en el sidebar (2026-08-14)

Reportado por el usuario: pinear un proyecto creado por esta vista (con un ícono custom) no lo mostraba en `Pinned` del sidebar ni usaba su ícono. Causa raíz: `toggleProjectPinned()` solo sabía **mostrar/ocultar** una fila que ya existiera — `findSidebarPinnedItem()` matchea por nombre contra los `<li class="project-item">` del sidebar, que son markup estático escrito a mano para los 3 proyectos mock originales. Un proyecto creado después nunca tuvo una fila ahí para empezar, así que "pinear" no encontraba nada que mostrar.

- **Fix**: nueva `ensureSidebarPinnedItem(project)` — si `findSidebarPinnedItem` no encuentra fila, crea un `<li>` nuevo y lo agrega al `<ul class="project-list">` de Pinned, usando **`project.icon`** (el que se eligió en el picker) en vez de un folder hardcodeado. `toggleProjectPinned()` la llama solo al pinear (`project.pinned === true`); al despinear sigue usando `findSidebarPinnedItem` tal cual, no hay razón para crear una fila solo para ocultarla.
- **Deliberadamente minimal**: la fila nueva es solo ícono + nombre — **sin** el crossfade folder↔folder-open (es una animación específica de la metáfora de carpeta para expandir/colapsar chats anidados; un ícono custom la reemplaza directamente, no anima entre dos estados) y **sin** sublista de chats (un proyecto recién creado no tiene ninguno). Reusa la clase `.folder-icon-closed` únicamente por su tamaño 16×16 (no hay una regla genérica de tamaño para un ícono suelto dentro de `.folder-icon`), sin su pareja `-open` — nunca crossfadea a nada, solo se ve como un ícono estático normal.
- **Gap resuelto (2026-08-14, pasada siguiente): la fila nueva ya tiene su elipsis.** `ensureSidebarPinnedItem()` ahora también genera el botón de acciones (`.sidebar-project-actions-wrap`/`-btn`) dentro de la fila y su popover portal (`sidebarProjectActionsMenuMarkup(id)`, agregado a `<body>`, mismo patrón que los 3 originales) — mismas 4 acciones (Unpin/Invite members/Archive/Edit detail), mismo look.
  - **No podía reusar `initSidebarProjectActions()` de `shell.js` tal cual** — esa función re-consulta TODOS los `[data-sidebar-actions-trigger]` de la página cada vez que corre, así que volver a llamarla le habría agregado un segundo listener de click a los 3 triggers originales (doble apertura/cierre por click). Se escribió `wireDynamicSidebarActionsTrigger(trigger, menu)` acá mismo — mismo mecanismo de abrir/cerrar/posicionar (portal, `position: fixed` calculado con `getBoundingClientRect()`, abre a la derecha con fallback a la izquierda) pero acotado a un solo par nuevo, sin tocar el wiring de los 3 originales.
  - **Los cierres globales (click afuera, scroll, resize) sí son gratis** — `closeAllSidebarActionMenus()` de `shell.js` consulta por clase (`.sidebar-project-actions-menu.is-open`), no por una lista fija capturada al iniciar, así que ya cierra el popover nuevo también sin que haga falta agregar un segundo listener global.
  - **La lógica de cada acción** (qué hace Unpin/Archive/Edit detail al clickear) se extrajo a `wireSidebarActionsMenuBehavior(menu)` — antes vivía inline dentro de `wireSidebarProjectActionsBehavior()` (el loop que wireaba los 3 originales al cargar la página); ahora ese loop y `ensureSidebarPinnedItem()` llaman a la misma función, para que las acciones nunca diverjan entre una fila estática y una dinámica.
  - **"Invite members" sigue siendo no-op acá también** — mismo estado que en los 3 originales (ver `sidebar.md` → Pendiente), no se agregó el formulario inline solo para la fila nueva.

### Bug corregido: cards nuevas (sin tags) salían con un padding-bottom distinto (2026-08-14)

Reportado por el usuario: las cards de proyectos creados por esta vista (que arrancan con `tags: []`) se veían más grandes/con más espacio abajo que las de los 3 proyectos mock originales (que sí tienen tags).

- **Causa raíz**: `.project-card` no era un contenedor flex — era un bloque normal con `min-height: 124px`. Con tags, el contenido natural de la card ya alcanza (o casi) esos 124px. Sin tags, `.project-card-tags` queda vacío (0 de alto, solo su `margin-top: 10px`), el contenido natural total cae por debajo de 124px, y en un bloque normal el `min-height` extra se agrega **después del último hijo** — en este caso, después de `.project-card-footer`, no distribuido en ningún otro lado. Eso es exactamente lo que se leía como "padding-bottom más grande": no era padding, era el propio `min-height` empujando espacio en blanco debajo del footer.
- **Fix**: `.project-card` pasó a `display: flex; flex-direction: column`, y `.project-card-tags` ganó `flex: 1` (+ `align-content: flex-start` para que los tags, si hay, se mantengan arriba dentro de esa fila en vez de centrarse). Ahora esa fila es la que absorbe el espacio de sobra del `min-height` — el footer queda siempre a la misma distancia fija del borde inferior de la card, con o sin tags, en vez de que el espacio extra se acumule después de él.
- **`.project-skeleton-card` no necesitó el mismo fix** — siempre renderiza la misma cantidad de placeholders (ícono, línea, 2 tag-pills, badge, avatar), así que su alto natural ya es consistente entre instancias; el bug era específico de datos reales con `tags.length` variable, no del esqueleto de carga.

### Archive → tab "Archived" → Delete (2026-08-14)

Pedido explícito del usuario, aclarando el comportamiento esperado de "Archive project" (que hasta ahora solo hacía desaparecer el proyecto de todos lados, sin ningún lugar para volver a verlo): agrega una 4ª tab de ownership, **"Archived"**, y una acción real de eliminar permanentemente desde ahí.

- **Nueva tab `data-ownership="archived"`**, junto a All/Created by you/Shared projects — mismo componente visual (`.projects-ownership-tab`), mismo wiring genérico ya existente (lee `tab.dataset.ownership`, no hizo falta tocar el listener).
- **`getFilteredProjects()` invierte el filtro de archivado en vez de ignorarlo**: todas las demás tabs seguían (y siguen) excluyendo proyectos archivados (`p.archived === false`); la tab Archived hace exactamente lo opuesto (`p.archived === true`) y **salta el filtro por owner** (un proyecto archivado conserva su `owner` original, pero ya no es relevante una vez archivado — mostrar todos los archivados sin importar quién lo creó). Búsqueda y tags siguen aplicando igual, combinables con la tab.
- **Archivar no cambia de tab automáticamente** — el proyecto se archiva y desaparece de la tab actual (fade-out existente, sin cambios), la tab Archived es un destino al que hay que entrar manualmente después, no una redirección forzada tras la acción.
- **El elipsis de una card archivada muestra un set de acciones distinto, no las 4 de siempre.** Pin/Unpin e Invite members no tienen sentido para algo en estado terminal "archivado" (un proyecto archivado no se muestra en el sidebar sin importar su `.pinned`; togglear pin desde acá podría resucitar silenciosamente una fila del sidebar que no debería estar visible) — así que una card archivada solo ofrece **Edit detail** (por si querés revisar antes de decidir) y **Delete project** (nuevo, destructivo). `projectActionsMenuMarkup(p)` ahora bifurca en `p.archived` para decidir cuál de los dos markups devolver.
- **"Delete project" abre un modal de confirmación** (`#deleteProjectModalBackdrop`), no elimina directo al click — mismo tratamiento visual (backdrop + blur + scale-in) que los otros modales de esta vista. Mensaje: `Are you sure you want to delete **{nombre}**? This action can't be undone.` CTAs: "Cancel" (`.btn-secondary`) y "Delete" (nuevo `.btn-danger`, en `shared/tokens.css` junto a `.btn-primary`/`.btn-secondary` — mismo shape, colores de los tokens `--color-danger`/`-hover` ya existentes, no uno nuevo inventado).
- **`deleteProject(id, tileEl)` es una eliminación de verdad, no un ocultamiento** — a diferencia de `archiveProject` (que solo pone `.archived = true` y oculta), esto hace `PROJECTS_DATA.splice(...)` y además **remueve del DOM** (no solo oculta) cualquier fila del sidebar y su popover portal asociados, porque no hay forma de "undelete" — dejarlos ocultos sería un elemento muerto acumulándose en el DOM para siempre. Misma micro-animación de fade+scale-down (160ms) que ya usa Archive antes de confirmar.
- **Nuevo modificador `.composer-menu-option--danger`** (en el `<style>` inline, junto al resto de `.composer-menu-option`) — ícono y label en `--color-danger` en reposo (no solo al hover), y el hover cambia de gris neutro a `--color-danger-tint`, para que "Delete project" no se lea como una opción más de la lista.
- **Empty state con copy dedicado**: `#projectsEmpty` ahora muestra "No archived projects." en la tab Archived en vez del genérico "No projects match your search." (que no tenía sentido ahí si nunca se buscó nada).

### Footer de la card/fila: badge de miembros en vez de avatar (2026-08-14)

Pedido explícito del usuario: reemplazar la foto de perfil (`user_profile.jpeg`, la misma imagen hardcodeada en las 3 filas mock) por un badge gris cuadrado que muestre cuántas personas tienen acceso al proyecto — ícono `users` + "N members" (singular "1 member" manejado también, aunque el usuario solo pidió ejemplos en plural).

- **Nuevo campo `members` en `PROJECTS_DATA`** — números mock fijos por proyecto (10 / 6 / 5), elegidos solo para que se vea variedad, no una regla de negocio. `createProject()` arranca todo proyecto nuevo en `members: 1` (solo el creador) — no hay flujo real de invitación que incremente este número todavía (ver "Invite members" en Pendiente, sigue siendo no-op).
- **`membersBadge(count)`** (nueva función, junto a `statusBadge()`) centraliza el markup y el singular/plural — un solo lugar en vez de repetir la lógica en `renderProjectsGrid` y `renderProjectsList`.
- **`.project-members-badge`**: mismo criterio "cuadrado, no circular/pill" que ya tenía el avatar (`--radius-sm`, no `999px`) — coherente con el badge de status (`.badge--square`) que queda al lado. Fondo `--color-surface-sunken`, texto `--color-ink-soft`, ícono `--color-ink-faint` — mismo peso visual bajo que el resto de metadata secundaria de la card, no compite con el nombre del proyecto.
- **Skeleton actualizado y renombrado**: `.project-skeleton-avatar` (24×24, pensado para una foto cuadrada) pasó a `.project-skeleton-members` (64×20) — si se dejaba en 24px, el swap skeleton→contenido real hubiera saltado de ancho apenas cargara el badge real (más ancho que un cuadrado de avatar).
- **`user_profile.jpeg` deja de referenciarse** en esta vista (sigue existiendo en `assets/img/`, sin uso conocido en el resto del prototipo por ahora).

- **El sidebar's `Pinned`**: markup estático por proyecto (ver `sidebar.md`) — un proyecto recién pineado ya genera su fila completa, ícono + elipsis con las 4 acciones incluido (ver bug fix arriba). Lo único que sigue sin resolver ahí es "Invite members" (mismo gap que las 3 filas originales).
- **`SEARCH_DATA.projects`** (Cmd+K): lista hardcodeada, ya documentada como intencionalmente desacoplada de `PROJECTS_DATA` desde antes de esta pasada — un proyecto nuevo (ni su ícono) no aparece en el buscador global. No se tocó para no romper esa decisión previa sin que se pida explícitamente.
- **Sin validación de nombre duplicado a nivel de UX** (el slug sí se desambigua internamente, pero dos proyectos podrían mostrarse con el mismo `name` visible sin ningún aviso).

## Project detail — construido y luego revertido (2026-08-14)

Se construyó una vista de detalle propia por proyecto (clic en el cuerpo de una card/fila → `#projectDetailView`, breadcrumb "Projects / {nombre}", título con ícono + conteo de miembros, botón Share placeholder, elipsis con Pin/Unpin, Edit detail, Archive y Delete siempre juntas). El mismo día, el usuario pidió revertirlo por completo — "esa vista ya no existe". Revertido en su totalidad: markup (`#projectDetailView` y su contenido), CSS (`.project-detail-*`), JS (`openProjectDetail`, `renderProjectDetail`, `projectDetailActionsMenuMarkup`, `membersLabelText`, toda la wiring asociada), el `cursor: pointer` que se le había agregado a `.project-card`/`.project-list-row`, y las referencias añadidas en `onNavSectionChange`/`closeAllProjectActionMenus`/`saveProjectEdit`/el modal de Delete. Las cards/filas volvieron a ser tiles estáticos sin destino, como antes de esta pasada — ver la sección "Create project" de arriba para el estado actual real de las cards.

Queda esta nota en vez de borrar el registro por completo, siguiendo la misma convención que ya usa `login.md` para pasadas revertidas — así no se repite el mismo camino sin querer más adelante.

## Clic en una card/fila: abre el chat del proyecto (2026-08-14)

Reemplazo real de lo anterior — clickear el cuerpo de una card/fila (no su elipsis) ya no lleva a una página de detalle, abre **el chat propio de ese proyecto**. Secuencia pedida explícitamente por el usuario, aclarada con una pregunta de por medio (ver `openProjectChat()`):

1. **Colapsa el sidebar de navegación** si estaba expandido (`#sidebar.is-collapsed` — mismo mecanismo que ya usa el botón real, idempotente si ya estaba colapsado).
2. **Entra directo al hilo de chat con cero mensajes**, no a la pantalla de saludo completa (`#chatEmpty`) — decisión tomada tras una ronda de aclaración: generalizar el mecanismo de 370px+panel (`has-panel`, hoy exclusivo de `#chatThread`) para que también aplicara sobre `#chatEmpty` hubiera significado tocar bastante código ya afinado (ver `chat-side-panel.md`, varios bugs ya encontrados/corregidos ahí). En cambio, se construyó un saludo propio y más angosto **dentro** de `#chatThread`.
3. **El panel lateral derecho se abre solo**, con este proyecto ya cargado como su único tab — mismo efecto que si el usuario lo hubiera elegido a mano desde el picker "What do you want to open?" del panel.

### El saludo angosto (`#chatThreadGreeting`)

Pedido explícito, con una segunda ronda de precisión sobre qué mantener y qué no:

- **Mantiene**: el ícono del agente (más chico, 32px vs. 48px del saludo completo) y el titular animado "What do you want to build?" con su mismo comportamiento — shine letra por letra + ciclo de palabras (build/audit/operate). `initGreetingShine()` se generalizó para aceptar un id de headline en vez de tener `#chatGreetingHeadline` hardcodeado, y ahora se llama dos veces (una por cada headline) — evita una segunda copia de esa lógica que se hubiera desincronizado.
- **Tipografía más chica**: `16px` (vs. `26px` del saludo completo) — pensado para el ancho angosto de 370px.
- **No incluye**: la barra gris de selector de proyecto (igual de invisible que siempre en el composer docked — ya estaba en `display: none` por CSS, no hubo que tocar nada ahí) ni los suggestion chips — el contexto de proyecto ya está establecido por cómo se llegó acá, así que volver a preguntar "select a project" no aplica.
- **Subtítulo propio, no el genérico de `#chatEmpty`** (pasada de refinamiento, mismo día): en vez de "Tell me the financial scenario…", `#chatThreadGreetingSubtitle` dice `"Ask me to dig into a specific part of {nombre del proyecto}."` — pedido explícito del usuario, invita a algo concreto sobre *ese* proyecto en particular, en vez de un texto genérico que no lo menciona. Se setea en `openProjectChat()`, junto con el resto del contenido del saludo.
- **El campo de texto es el composer docked de siempre** (`#dockedInput`), sin ningún campo nuevo — "comprimido" ya lo describe bien: es exactamente el mismo composer que aparece una vez el hilo tiene mensajes.
- **Bug corregido: el brillo del titular no se veía.** La animación letra por letra (`@keyframes letterShine`) estaba scopeada al selector `.chat-empty h1 .glow-letter` — como el titular angosto es un `<h2>` dentro de `.chat-thread-greeting`, no de `.chat-empty`, `initGreetingShine()` generaba los `<span class="glow-letter">` correctamente pero la regla CSS nunca los matcheaba, así que nunca animaban. Fix: el selector ahora cubre ambos (`.chat-empty h1 .glow-letter, .chat-thread-greeting h2 .glow-letter`).

### Primer mensaje real: mismo comportamiento que "inicializar un chat"

`sendFromDocked()` ahora detecta si `#chatMessages` está vacío (cero mensajes) y, si es así, llama a `startChat(text)` en vez de `continueChat(text)` — el primer mensaje tipeado en este flujo dispara el mismo título derivado, `registerNewChat()`, y la respuesta canned "completa" (900ms + resumen de reconciliación), no el "Done, updated." más corto de un follow-up. `startChat()` también oculta `#chatThreadGreeting`/muestra `#chatMessages` de nuevo — no-ops inofensivos en el flujo normal (saludo completo → primer mensaje), donde ninguno de los dos estaba tocado.

### Header del hilo: "{Proyecto} / New chat"

`contextState.empty.selected` se setea directamente al abrir (mismo campo que ya usa el selector de contexto real, sin pasar por `selectContext()` porque ese trigger UI no existe en este flujo) — `updateChatThreadHeader()` y `registerNewChat()` ya leen de ahí, así que el breadcrumb y el chat registrado en Recent quedan asociados al proyecto correcto sin lógica nueva. Llamado con `firstPrompt` vacío, el label del chat cae en "New chat" (mismo fallback que ya tenía `deriveChatTitle('')`).

### El tab del proyecto en el panel: textura de mapa

`renderPanelTabContent()` agrega `.panel-tab-content--map` cuando el tab activo es de tipo `project` — fondo con grid de puntos (`radial-gradient` repetido, técnica típica de canvas/whiteboard) para leerse como una vista de mapa/flow, no como el texto plano placeholder que sigue usando Apps/Agents. **El contenido real de qué va dentro de esa vista sigue sin definir** — el usuario aclaró explícitamente que esto es solo la textura de fondo, el contenido viene en una pasada posterior.

### Pendiente / abierto

- **Contenido real del tab de proyecto**: por ahora solo tiene la textura de mapa + el texto placeholder de siempre ("Project view — {nombre}") encima. Qué va efectivamente dentro (¿un builder visual? ¿un dashboard?) queda para la próxima instrucción del usuario.
- Mismo mecanismo de un solo tab: `resetPanelTabs()` se llama antes de abrir, así que si el panel ya tenía otros tabs abiertos (de una sesión de chat anterior), se pierden — coherente con "empezar un chat nuevo para este proyecto", pero vale confirmarlo si alguna vez se quiere que convivan.

## Archivos relacionados

- `flows/home/index.html` — markup + JS del home completo
- `shared/tokens.css` — estilos de shell (sidebar) reutilizados acá
- `shared/shell.js` — collapse de sidebar, expand de proyectos
