# Chats and Tasks

> Última actualización: 2026-08-18
> Archivo relacionado: `flows/home/index.html` (markup + `<style>` + script, todo inline al final del archivo)
> Ver también: [`sidebar.md`](./sidebar.md) → nav item "Chats and Tasks" (dispara esta vista), [`home.md`](./home.md) → vista Projects (el patrón visual que esta vista replica)

## Propósito

Vista global de todos los chats de la cuenta, sin el recorte que ya aplican **Pinned** (chats anidados dentro de sus proyectos) y **Recent** (chats sueltos sin proyecto) en el sidebar — a diferencia de esas dos, acá se listan *todos*, con o sin contexto. Se abre desde el nav item del sidebar, que hasta la primera pasada solo mostraba el empty state genérico compartido con Apps/Agents.

**Nota de alcance (2026-08-13):** el concepto de "Task" no está definido todavía — por instrucción explícita del usuario, esta vista solo modela **Chats**. No hay ningún tratamiento visual de "Task" en la UI. Cuando ese concepto se defina, esta vista es donde debería aparecer.

**Rebautizado a "Chats" (2026-08-13, quinta pasada).** El label del nav item, su tooltip y el `<h1>` de esta vista pasaron de "Chats and Tasks" a **"Chats"** — pedido explícito del usuario para sacar la palabra "task" del copy visible mientras el concepto siga sin definir. El nombre interno (`data-nav="chats-tasks"`, este archivo `chats-and-tasks.md`, nombres de función/variable en el código) se mantiene sin cambios — es el identificador de la sección, no copy que el usuario vea.

## Modelo de datos: qué es un chat acá

Un chat es el inicio de una conversación con un agente. Puede:
- estar **suelto** (sin ningún contexto asociado), o
- estar asociado a un **Project**, una **App**, o un **Agent** — los mismos tres tipos que ya modelan el selector de contexto gris del composer y el picker del panel lateral del chat (`PROJECTS_DATA`/`PANEL_APPS_DATA`/`PANEL_AGENTS_DATA`).

Un chat nunca "es" de naturaleza project/app/agent — el contexto es un atributo del chat, no una categoría exclusiva de chat. Eso es justo lo que `CHATS_DATA` modela: cada entrada tiene `context: { type: 'project'|'app'|'agent', id } | null`.

`CHATS_DATA` consolida en un solo array los 6 nombres de chat que ya existían repartidos en dos lugares sin relación entre sí (markup estático de Pinned/Recent en el sidebar, y `SEARCH_DATA.chats` del modal Cmd+K — ninguno de los dos tenía un campo de contexto real). Se agregaron 2 chats nuevos (`Google Drive sync check`, `Period close agent review`) únicamente para que las filas de contexto App y Agent tuvieran al menos un ejemplo real — el resto de los nombres ya existía en el prototipo. `createdAt` es texto fijo por ítem ("2 days ago", "1 week ago"...), no una fecha real calculada — mismo criterio de mock data estática que el resto del prototipo.

**No conectado a otras fuentes de datos — con una excepción nueva (2026-08-14).** Igual que `PROJECTS_DATA` vs. el markup estático de Pinned, o `SEARCH_DATA` vs. `PROJECTS_DATA`: los 6 nombres mock originales de `CHATS_DATA` no derivan ni sincronizan con Pinned/Recent/Search, y archivar/renombrar un chat acá tampoco toca esos otros lugares. La excepción es hacia adelante, no hacia atrás: **arrancar un chat nuevo** (`registerNewChat`, ver `docs/chat.md`) sí hace `CHATS_DATA.unshift(...)` **y** agrega la fila equivalente al Recent del sidebar en el mismo momento — así que, de acá en más, un chat recién creado existe consistentemente en ambos lugares desde el arranque. Lo que sigue sin sincronizarse son los cambios *posteriores* (archivar, renombrar, pinear) y los 6 chats mock que ya existían antes de este mecanismo.

## Decisiones tomadas

- **Mismo patrón visual que la vista Projects, pero simplificado.** Título + buscador en el header, sin Filter (no hay tags en un chat) y sin toggle grid/list (solo lista, por instrucción explícita — un chat es una fila, no una card). Clases propias (`.chats-*`, `.chat-list-row*`), copiadas 1:1 del look de `.projects-view-header`/`.project-list-row` en vez de compartir el nombre de clase — este archivo ya se golpeó dos veces con colisiones de clase entre componentes visualmente iguales pero funcionalmente independientes (`.project-row`, `.search-tab`; ver `home.md`), así que cada vista nueva parte de clases propias por default.
- **Click en una fila: abre el chat de verdad** (ver sección "Click en una fila abre el chat de verdad" más abajo) — decisión original (no-op, mismo criterio que las cards de Projects/Search) revertida el 2026-08-18 a pedido explícito del usuario, específicamente para esta vista. Las filas de Pinned/Recent del sidebar siguen siendo no-op, sin cambios.
- **Campos por fila**: ícono de chat (`message-circle-more`, el mismo que ya usa "New Chat" y el header del hilo sin contexto) + nombre del chat + ícono y nombre del contexto (Project/App/Agent) si tiene, en blanco si no tiene + fecha de creación (texto fijo). El ícono de contexto reusa `iconForItem()`/`findPanelItem()`, las mismas funciones que ya resuelven esto para el header del hilo de chat y el panel lateral — ningún ícono nuevo, ninguna lógica de resolución nueva.
- **Selección masiva ("Select").** Botón al lado de "New Chat" que entra a un modo de selección: aparece un checkbox por fila, las filas se vuelven clickeables (toggle de selección, único lugar donde el click en una fila SÍ hace algo), la ellipsis por fila se oculta, y los controles del header (buscador, Select, New Chat) se reemplazan por una barra de "N selected" + Cancel + Archive. **Archive es la única acción masiva** — explícitamente no hay Delete ni "mover a proyecto". Sin vista de "Archived" para verlos después, mismo gap ya aceptado para el Archive de Projects.
- **"New Chat" del header de esta vista dispara el mismo botón de siempre** (`document.getElementById('newChatBtn').click()`) en vez de reimplementar el reset — un solo lugar de verdad para "qué pasa al iniciar un chat nuevo".
- **Ellipsis por fila: una sola acción, "Rename chat", visible pero sin implementar.** Por instrucción explícita, no abre ningún modal ni campo inline todavía — clickearla solo cierra el popover, mismo criterio placeholder que otras acciones no cableadas del prototipo (ej. "Edit detail" antes de tener su modal).
- **Loading skeleton, mismo tratamiento que Projects (2026-08-13, agregado en la segunda pasada).** `queryChatsWithLoading(delay)` centraliza el ciclo (oculta lista/empty reales, muestra el skeleton, espera `delay` ms, renderiza) — 700ms al entrar a la sección (`loadChatsView`), 400ms en cada re-búsqueda con debounce de 300ms (mismo criterio que Projects). Nunca se dispara en Select/Archive, mismo criterio ya usado por Projects para Pin/Unpin/Archive. Reusa el mismo `@keyframes skeletonShimmer` que ya usa el skeleton de Projects, con clases propias (`.chat-skeleton-*`) — no comparte nombre con `.project-skeleton-*`.
- **Reset del modo selección al entrar a la sección.** Cada vez que se navega a "Chats and Tasks" desde el nav, `setChatsSelectMode(false)` fuerza el estado por defecto — evita quedar "atrapado" en modo selección de una visita anterior sin darse cuenta.

## Pin / Unpin: impacta el Pinned del sidebar (2026-08-13, tercera pasada)

Nueva primera opción en la ellipsis de cada fila: **Pin chat / Unpin chat** (ícono `pin`/`pin-off` según `chat.pinned`), antes de "Rename chat". Pedido explícito del usuario: que fijar un chat acá **impacte directamente** el listado de Pinned del sidebar, y que un chat ya pineado tenga **las mismas acciones de la ellipsis disponibles directamente desde el sidebar**.

- **Un solo Pinned, no dos listas.** Pinear un chat lo agrega como fila nueva dentro del mismo `<ul class="project-list">` donde ya viven los 3 proyectos pineados — Pinned es una sola lista curada por el usuario (proyectos y chats mezclados), no una sección de "chats pineados" aparte debajo.
- **Mismo principio que `toggleProjectPinned`/`ensureSidebarPinnedItem`** (el mecanismo que ya existía para que un proyecto recién creado se pinee sin tener markup estático previo): `toggleChatPinned(id)` es el único punto de verdad, llamado tanto desde la ellipsis de esta vista como desde la ellipsis del sidebar — nunca dos implementaciones de "la misma acción". Como ningún chat tiene fila estática en el sidebar (a diferencia de los 3 proyectos originales), **todo chat pineado pasa por la rama dinámica**: `ensureSidebarPinnedChatItem(chat)` crea el `<li>` + su popover portal la primera vez que se pinea; unpinear solo lo oculta (`hidden`), no lo destruye — volver a pinear el mismo chat reutiliza el mismo `<li>`/popover en vez de duplicarlo.
- **La fila en el sidebar es un botón plano, sin expandir/colapsar** — a diferencia de los proyectos (folder↔folder-open + chats anidados), un chat no tiene sub-items que revelar. Mismo ícono de chat (`message-circle-more`) que el resto del prototipo.
- **Ellipsis del sidebar: popover propio por chat pineado** (portal, mismo motivo de overflow-clipping que ya obliga a los popovers de proyecto a vivir fuera del subárbol del sidebar — ver `sidebar.md`), con 2 acciones: **Unpin chat** (siempre esta label, nunca "Pin chat" — el popover solo existe una vez el chat ya está pineado, no hay ambigüedad que resolver) y **Rename chat** (llama a la misma función `renameChat()` que la ellipsis de esta vista — ver sección siguiente).
- **Clases 100% propias** (`.sidebar-pinned-chat-*`), viven en `shared/tokens.css` junto a `.sidebar-project-actions-*` — nunca comparten nombre con `.project-row`/`.sidebar-project-actions-*`, mismo criterio que el resto del archivo.
- **Todos los chats arrancan sin pinear** (`pinned: false` en `CHATS_DATA`) — a diferencia de los 3 proyectos mock (que ya nacen `pinned: true` porque así estaban en el sidebar desde el principio), acá no hay ningún estado previo que replicar, así que el punto de partida honesto es vacío.
- **Verificado end-to-end con Chrome headless** (no solo lectura de código): click real en la ellipsis → "Pin chat" → aparece en el sidebar con el nombre correcto → la ellipsis de esta vista refleja "Unpin chat" al reabrirse → click en la ellipsis del sidebar → "Unpin chat" ahí → la fila del sidebar se oculta. Los 3 tipos de contexto (Project/App/Agent) se probaron pineados a la vez.

## Rename chat: modal real (2026-08-13, cuarta pasada)

"Rename chat" deja de ser no-op — abre un modal real (`#renameChatModalBackdrop`/`.rename-chat-modal`) con el mismo tratamiento visual que el resto de los modales del prototipo (backdrop con blur, scale-in centrado, header con título + cerrar, footer con Cancel/acción primaria), pedido explícito del usuario ("con las definiciones que tenemos de modal").

- **Un solo campo**: input de texto "Chat name", pre-rellenado con el nombre actual y con foco + selección automática al abrir (para poder empezar a escribir el nombre nuevo inmediatamente). Reusa `.create-project-field`/`.create-project-field-label` — el mismo primitivo genérico de campo-con-label que ya reusa el modal de Edit project, nada nuevo que mantener.
- **"Save changes" arranca deshabilitado solo si el campo queda vacío** — mismo criterio mínimo de validación que el resto de los formularios del prototipo (Create project, Edit project). Enter en el input también dispara el guardado si está habilitado.
- **Una sola instancia compartida por las dos ellipsis** (la de esta vista y la del sidebar): `renameChat(id)` — la función que antes solo cerraba el popover — ahora llama a `openRenameChatModal(id)`, que busca el chat en `CHATS_DATA` y prellena el modal. Mismo principio que `openProjectEditModal`: construir el formulario una sola vez es lo que hace que renombrar funcione desde cualquiera de los dos entry points sin wiring adicional.
- **Actualiza el nombre directamente en `CHATS_DATA`** (`chat.name = ...`) al guardar, y re-renderiza la lista de esta vista (`applyChatsFilters()`). Si el chat está pineado, también empuja el nombre nuevo al `<span class="sidebar-pinned-chat-name">` de su fila en el sidebar — a diferencia del rename de Projects (que necesita buscar la fila del sidebar *por el nombre viejo antes* de sobreescribirlo, porque `findSidebarPinnedItem` matchea por texto), acá `findSidebarPinnedChatItem` matchea por `data-chat-id` — el id nunca cambia, así que no importa en qué momento se haga la búsqueda.
- **Verificado end-to-end con Chrome headless**: pinear un chat → abrir su ellipsis → "Rename chat" → modal abre pre-rellenado con foco automático → cambiar el texto → "Save changes" → el modal cierra, la fila de esta vista muestra el nombre nuevo, y la fila correspondiente en el sidebar Pinned también.
- **Fuera de alcance**: no valida nombres duplicados, no hay confirmación de "cambios sin guardar" al cerrar sin guardar (Cancel/X/Escape/click afuera descartan directamente, mismo criterio que el resto de los modales del prototipo).

## Tabs All / Archived (2026-08-13, quinta pasada)

Nuevas tabs debajo del header, arriba de la lista: **All** / **Archived** — pedido explícito del usuario, en anticipación a que se archiven chats desde acá (bulk Archive ya existía, pero no había ningún lugar para volver a verlos). Mismo patrón que las tabs de ownership de Projects (`.projects-ownership-tab`), simplificado a solo 2 (acá no hay concepto de "owner").

- **Clase propia** (`.chats-view-tab`), copiada 1:1 de `.projects-ownership-tab` en vez de reusada — mismo motivo de siempre: el modal Search tiene un wiring global sobre `.search-tab` que stompearía el estado activo de cualquier tab que comparta ese nombre.
- **`chatsTab` ('all' | 'archived')** es la segunda dimensión de filtro, combinada con la búsqueda en `getFilteredChats()`: `CHATS_DATA.filter(c => c.archived === showArchived)`. El buscador sigue funcionando dentro de cada tab (buscar en Archived busca solo entre los archivados).
- **Mensaje de empty state dedicado**: "No archived chats." en vez del genérico "No chats match your search." cuando la tab activa es Archived y no hay nada — mismo criterio que ya usa Projects para su propia tab Archived.
- **"Select" se oculta en la tab Archived** — no tiene sentido archivar en bulk algo que ya está archivado (la única acción masiva que existe hoy es Archive). Si el usuario estaba en modo selección y cambia de tab, `setChatsSelectMode(false)` lo saca automáticamente — evita dejar ids seleccionados de una lista que ya no se ve.
- **Ellipsis de un chat archivado: "Unarchive chat" + "Rename chat", sin Pin/Unpin.** Mismo criterio que Projects ya aplica a sus propias cards archivadas (ver comentario en `projectActionsMenuMarkup`): pinear algo en estado terminal "archivado" podría resucitar en silencio una fila del sidebar para un chat que ya no aparece en la tab All. `chatActionsMenuMarkup(c)` ahora ramifica por `c.archived`.
- **La tab seleccionada persiste entre visitas a la sección** (no se resetea a "All" al re-entrar) — mismo criterio que Projects, que tampoco resetea su filtro de ownership ni el término de búsqueda al volver a la vista. Solo el modo de selección masiva se resetea siempre (`setChatsSelectMode(false)` en `onNavSectionChange`).
- **Verificado end-to-end con Chrome headless**: archivar un chat vía Select → desaparece de All → aparece en Archived → "Select" está oculto en Archived → volver a All lo sigue mostrando correctamente excluido.

## Unarchive chat (2026-08-18)

Pedido explícito del usuario: agregar una opción en la ellipsis de un chat archivado para desarchivarlo — hasta esta pasada era un gap conocido y documentado (ver historial), un chat archivado se quedaba ahí permanentemente.

- **`unarchiveChat(id)`**, mismo shape que `unarchiveProject` (Projects ya tiene esta misma acción — ver `projectActionsMenuMarkup`/`unarchiveProject`): pone `chat.archived = false`, y si el chat seguía marcado `pinned` (archivar nunca borra ese flag, solo oculta la fila del sidebar — ver `archiveSelectedChats`), restaura esa fila en vez de dejarla oculta para siempre (`ensureSidebarPinnedChatItem` encuentra la existente y la reusa, o crea una nueva en el caso improbable de que no exista).
- **Ícono `archive-restore`**, el mismo que ya usa "Unarchive" en Projects — reuso directo, no uno nuevo. Label "Unarchive chat" (con el sufijo "chat") para quedar consistente con el resto de las acciones de esta ellipsis ("Pin chat"/"Rename chat"), aunque Projects use solo "Unarchive" a secas.
- **Persistencia real**: `update chats set archived = false where id = ...` contra Supabase, mismo patrón fire-after-mutate que el resto de las escrituras de esta vista (mutar en memoria + re-renderizar primero, disparar el `update` después, sin rollback si falla — ver `docs/supabase.md`).
- **Al desarchivar, el chat desaparece de la tab Archived** (queda filtrado por `getFilteredChats()`, que exige `archived === true` ahí) y vuelve a aparecer en All — sin toast ni confirmación, mismo criterio silencioso que el resto de las acciones de este prototipo.
- **Sin verificación end-to-end con sesión real** (mismo límite de siempre en este entorno) — validado por sintaxis y consistencia con el `unarchiveProject` ya probado de Projects.

## Verificación de Archive + bug corregido: el popover de la ellipsis se cortaba (2026-08-18)

Pedido explícito del usuario: verificar que archivar un chat desde esta vista funciona con una sola acción (sin necesitar repetirla) y revisar el popover de la ellipsis, que se veía cortado cuando quedaban pocos chats en la lista (1 o 2).

- **Archive: confirmado correcto, una sola acción es suficiente.** `archiveSelectedChats()` pone `c.archived = true` una sola vez por chat seleccionado; `getFilteredChats()` lo excluye de inmediato de la tab All (`c.archived === showArchived`) y lo incluye en Archived — no hace falta ninguna acción repetida ni hay ningún estado intermedio.
- **Bug real encontrado y corregido: el popover de la ellipsis (`.chat-actions-menu`) se cortaba con `#chatsList`.** Vivía `position: absolute` anidado dentro de `.chat-list-row-actions-wrap`, y `#chatsList` tiene `overflow: hidden` (para sus esquinas redondeadas) — con pocas filas, la caja de `#chatsList` es baja, y cualquier parte del popover que cayera fuera de esa caja (aunque hubiera espacio de sobra más abajo en el viewport real) se recortaba silenciosamente. Mismo patrón de bug ya resuelto varias veces en este archivo para otros popovers (el de acciones de Projects, el de Pinned del sidebar).
- **Fix: convertido en portal.** Un solo elemento compartido (`#chatActionsMenu`, vive como hermano de `.app-shell` cerca de `</body>`, no uno por fila) en vez de uno embebido por fila — como la lista entera se re-renderiza en cada filtro/búsqueda/cambio de tab de todos modos, no hay ninguna ventaja en tener N copias estáticas. `openChatActionsMenu(trigger, chat)` arma el contenido (Pin/Unpin o Unarchive según `chat.archived`, + Rename) y calcula la posición con `position: fixed` vía `getBoundingClientRect()` del botón — abre hacia abajo, alineado al borde derecho del botón (misma dirección que antes), con flip hacia arriba si no entra por debajo del viewport. Mismo mecanismo ya usado para los popovers de la sidebar.
- **Las acciones del popover (Pin/Unpin, Unarchive, Rename) ganaron su propio listener** directamente en `#chatActionsMenu`, ya que al vivir fuera de `#chatsList` los clics ahí ya no llegan a la delegación de la lista.
- **Cierre defensivo en scroll/resize** agregado (`document.addEventListener('scroll', ..., true)` + `window.addEventListener('resize', ...)`) — la posición es una foto fija tomada al abrir, mismo criterio que los demás popovers portal de este archivo.
- **Verificado con Chrome headless**: extrayendo solo el `<style>` real del archivo (sin pasar por el guard de sesión de Supabase) se confirmó `position: fixed`, `z-index: 200` en el popover — ya no depende del `overflow: hidden` de ningún ancestro. No se pudo probar el flujo de click real de punta a punta (sin sesión contra el Supabase real en este entorno).

## "Archive chat" agregado a la ellipsis (2026-08-18, misma fecha)

El párrafo anterior verificó que el mecanismo de Archive (bulk, vía "Select") funciona con una sola acción — pero el usuario reportó, sobre esa misma verificación, que **no había ninguna forma de archivar un chat individual desde su propia ellipsis** — la única entrada era entrar a modo selección. Gap real: Projects sí tiene "Archive project" directo en la ellipsis de cada card, además de cualquier acción masiva; Chats no tenía el equivalente.

- **`archiveChat(id)`** (nueva función, junto a `unarchiveChat`): versión de un solo chat de lo que `archiveSelectedChats()` ya hacía en bulk — pone `chat.archived = true`, oculta la fila del sidebar si estaba pineado, re-renderiza, persiste con `update` contra Supabase. Sin animación de fade (`archiveSelectedChats` tampoco tiene una — se mantuvo consistente dentro de esta vista en vez de que solo uno de los dos entry points tuviera movimiento).
- **Nueva opción "Archive chat" en `chatActionsMenuButtonsMarkup(c)`**, al final del set de un chat no archivado (Pin/Unpin chat, Rename chat, **Archive chat**) — mismo orden que usa Projects (Archive al final).
- **`data-chat-archive` wireado en el listener de `#chatActionsMenu`** (el portal descrito arriba), junto a Pin/Unarchive/Rename.
- **Sin verificación end-to-end con sesión real** (mismo límite de siempre en este entorno) — validado por sintaxis y consistencia con `archiveProject`/`archiveSelectedChats`.

## Delete chat (2026-08-18, pedido explícito del usuario)

"Delete chat" — eliminación real y permanente, con modal de confirmación (copy exacto pedido por el usuario, traducido al inglés como el resto del prototipo).

- **Scope: chats activos y archivados por igual** — primera versión lo restringió a archivados (mismo criterio de dos pasos que Projects: archivar primero, borrar solo alcanzable desde ahí), pero el usuario aclaró explícitamente que no quiere ese requisito para chats. `chatActionsMenuButtonsMarkup(c)` ahora agrega **Delete chat** al final de las dos ramas: Unarchive chat / Rename chat / **Delete chat** para archivados, y Pin-Unpin chat / Rename chat / Archive chat / **Delete chat** para activos — siempre roja (`.composer-menu-option--danger`, mismo modificador que ya usa "Delete project"), siempre al final. `deleteChat(id)` no necesitó ningún cambio para esto — ya limpiaba la fila del sidebar sin asumir que el chat estuviera archivado.
- **Modal propio** (`#deleteChatModalBackdrop`/`.delete-chat-modal`, clase independiente copiada 1:1 del shell visual de "Delete project" — backdrop con blur, scale-in, header/body/footer), con el copy pedido:
  - Título: **"Delete chat"**
  - Cuerpo: **"Are you sure you want to delete this chat?"** — texto genérico, sin el nombre del chat (a diferencia de "Delete project", que sí interpola `<strong>${project.name}</strong>` — se siguió la redacción literal que pidió el usuario en vez de agregar esa variante).
  - Footer: **Cancel** (`.btn-secondary`) + **Delete** (`.btn-danger`, rojo).
- **`deleteChat(id)`** (nueva función, junto a `unarchiveChat`): mismo shape que `deleteProject` — a diferencia de Archive (que solo oculta), esto realmente saca el chat de `CHATS_DATA` (`.splice`) y remueve por completo (no solo `.hidden = true`) su fila del sidebar Pinned y el popover portal asociado, si existían — no hay forma de "des-borrar", así que no tiene sentido dejar elementos muertos colgando en el DOM. Persiste con `.delete()` real contra Supabase.
- **El botón de la ellipsis solo abre el modal** (`openDeleteChatModal(id)`) — `deleteChat()` recién corre si se confirma con "Delete"; Cancel, la X, click afuera o Escape lo descartan sin tocar nada, mismo mecanismo que el resto de los modales del prototipo.
- **Verificado con Chrome headless** (extrayendo el `<style>` real + `shared/tokens.css`, sin pasar por el login): el modal abre con `opacity`/`transform` en su estado final, título y cuerpo muestran el texto exacto pedido, y el botón de confirmar computa `rgb(196, 58, 58)` (`--color-danger`) de fondo. No se pudo probar el flujo de borrado real (click confirmar → `CHATS_DATA.splice` → `delete` contra Supabase) de punta a punta, sin sesión real en este entorno.
- **Líneas divisorias del modal removidas** (pedido explícito, misma pasada): `border-bottom` del header y `border-top` del footer sacados de `.delete-chat-modal-header`/`-footer` — a diferencia del resto de los modales del prototipo (Delete project, Rename chat, etc.), que sí las mantienen. Decisión puntual de este modal, no un cambio al primitivo compartido.
- **Border-radius, confirmado consistente en los 8 modales del prototipo** (pregunta del usuario, reconfirmada una segunda vez el mismo día): los 8 (`.project-edit-modal`, `.create-project-modal`, `.delete-project-modal`, `.rename-chat-modal`, `.delete-chat-modal`, `.app-edit-modal`, `.delete-app-modal`, `.search-modal`) usaban en ese momento **`border-radius: var(--radius-lg)`** (20px, definido una sola vez en `shared/tokens.css`) — mismo valor en todos, sin excepciones. **Nota: este valor cambió horas después** (ver el punto siguiente) — el estado final vigente es `var(--radius-md)` (12px), no el 20px confirmado acá.
- **Padding del header/body ajustado** (pedido explícito del usuario, misma pregunta sobre el border-radius): con las líneas divisorias ya removidas (ver arriba), el espacio entre el título y la descripción quedó en 36px (16px de `padding-bottom` del header + 20px de `padding-top` del body) flotando sin nada que lo justifique visualmente — se sentía "mucho espacio". Sin el divisor, la separación tiene que venir del padding, no de una línea, así que se ajustó a menos: `.delete-chat-modal-header` pasa a `padding: 16px 20px 8px` y `.delete-chat-modal-body` a `padding: 8px 20px 20px` (ambos bajan su lado que da hacia el otro bloque a 8px, el resto de sus paddings queda igual). Puntual de este modal — los que sí conservan la línea divisoria (Delete project, etc.) no la necesitan, la línea ya separa los bloques. Verificado con Chrome headless: la distancia real entre el borde inferior del título y el borde superior del texto del body bajó de la cifra original a **17px**.
- **Border-radius bajado un nivel en los 8 modales** (pedido explícito del usuario, mismo día): de `var(--radius-lg)` (20px) a `var(--radius-md)` (12px) — un escalón hacia abajo en la escala de tokens (`--radius-sm: 8px` / `-md: 12px` / `-lg: 20px`, `shared/tokens.css`), aplicado por igual a `.project-edit-modal`, `.create-project-modal`, `.delete-project-modal`, `.rename-chat-modal`, `.delete-chat-modal`, `.app-edit-modal`, `.delete-app-modal` y `.search-modal` — se mantiene la consistencia entre los 8, ninguno quedó en un valor distinto. No se tocaron los otros 3 usos de `--radius-lg` que no son modales (`.chat-input-shell`, `.project-select-bar`, `.section-empty-icon`) — siguen en 20px, ese cambio fue puntual a la familia de modales. Verificado con Chrome headless: los 8 computan `border-radius: 12px`.

## Empty state ilustrado: "No chats yet" (2026-08-18)

Pedido explícito del usuario, aclarado en varias rondas (la primera descripción llegó con bastante ruido de dictado): un empty state real para cuando el usuario **no tiene ningún chat todavía** — no la caja de texto simple que ya existía para "no hay resultados de tu búsqueda"/"no hay archivados".

- **Tres estados distintos, no uno solo.** `renderChatsList` ahora calcula `isGenuinelyEmpty = chatsTab === 'all' && !chatsSearchTerm.trim() && chats.length === 0` — solo ese caso muestra el hero ilustrado (`#chatsEmptyHero`). Buscar y no encontrar nada, o estar en la tab Archived sin nada archivado, siguen mostrando la caja de texto de siempre (`#chatsEmpty`) — no tiene sentido invitar a "crear tu primer chat" cuando en realidad sí hay chats y el filtro no encontró ninguno.
- **Ícono central: `assets/img/descarga.png`**, un asset que ya estaba en el repo sin usar (agregado el 2026-08-12 por otra sesión/el usuario, nunca referenciado hasta ahora) — un remolino de 4 pétalos en gradiente azul→violeta→rosa. No es un ícono de Lucide, es una imagen real dentro de un cuadrado (`.chats-empty-core`, `border-radius: var(--radius-lg)`, 72px) con un shadow de dos capas en `rgba()` (índigo + rosa) sampleado a mano de los propios colores del asset — no hay (ni tiene sentido crear) un token `--color-*` para "el gradiente exacto de esta imagen puntual"; mismo tipo de excepción que ya contemplan las leyes de diseño para el acento "AI gradient".
- **4 cards decorativas, en un arco fijo arriba/alrededor del ícono** (revisado el mismo día, ver abajo) — representan las acciones dentro de la aplicación: **app** (extremo izquierdo, `layout-grid`), **chat** (interior izquierdo, `message-circle-more`), **agent** (interior derecho, `bot`), **project** (extremo derecho, `folder`). **Corrección pedida por el usuario sobre la primera versión**: originalmente el extremo derecho repetía "app" (dos cards de app, espejadas para simetría) — el usuario pidió que en vez de repetir app hubiera una card de **Project**, para que las 4 cubran los 3 tipos de contexto reales de un chat (`project`/`app`/`agent`, mismos que modela `CHATS_DATA`) más el chat mismo, uno de cada uno. Clase renombrada de `.chats-empty-card--app-a` a `.chats-empty-card--project` (la posición en el arco no cambió, solo el ícono y el nombre de la clase); `.chats-empty-card--app-b` pasó a llamarse `.chats-empty-card--app` ya que ahora es la única card de app.
- **Tamaño del CTA, ajustado a pedido del usuario**: `.chats-empty-cta` usa `height: 32px; padding: 0 14px` (más íconos a 14px) — exactamente el mismo tamaño que ya usa el "New chat" del header (`.chats-view-header .btn-primary`), reusado literal en vez de dejarlo con el tamaño default de `.btn-primary` (que se veía más grande que el resto de botones pequeños del prototipo). Verificado con Chrome headless: los dos botones computan `height: 32px`, `padding: 0px 14px`, idénticos.
- **Revisión el mismo día, pedido explícito del usuario: se sacó toda la coreografía de hover.** La primera versión escondía el botón "New chat" hasta pasar el mouse (cards se separaban, remolino se desvanecía, CTA aparecía en su lugar) — el usuario la pidió puntualmente en esa ronda y después la quiso quitar. Ahora:
  - **Las 4 cards ya no se mueven con hover** — quedan fijas, formando un **arco por encima/alrededor del ícono** (nunca una card justo debajo — ese espacio queda limpio para el título y la descripción). Posiciones recalculadas sobre un arco de radio ~80px con ángulos ±25°/±70° desde la vertical, en vez de la cruz original (arriba/derecha/abajo/izquierda) que sí tenía una card justo debajo.
  - **El ícono (el remolino) tiene ahora su propia animación permanente**, no ligada a hover: gira un poco y encoge un poco, en loop suave (`@keyframes chatsEmptyCoreIconPulse`, `rotate(-8deg) scale(.9)` en el punto medio, 4.5s `ease-in-out infinite`) — pedido explícito ("que gire un poquito y disminuya un poco el tamaño"). Duración fuera del canon 120/200/320ms a propósito: ese canon es para micro-interacciones que responden a un estado, esto es un loop ambiental — misma excepción que ya tiene documentada el shimmer del skeleton.
  - **El botón "New chat" pasó a flujo normal**, como último elemento dentro de `.chats-empty-hero`, debajo del `<h2>`/`<p>` — siempre visible, ya no `position: absolute` ni condicionado a `:hover`/`:focus-within`. Sigue reenviando el click a `#newChatBtn`, sin cambios ahí.
- **Verificado con Chrome headless**: capturas de pantalla del estado final — arco de 4 cards sin overlaps, ninguna card debajo del ícono, ícono con su rotación visible a mitad del loop (confirma que la animación corre), botón "New chat" siempre presente debajo del texto con el mismo tamaño que el del header. No se pudo probar el flujo de "clic → abre new chat" de punta a punta contra una sesión real en este entorno.
- **Bug ya conocido, buscado y no encontrado en ninguna de las dos pasadas**: se revisó el comentario HTML en busca de la secuencia `-->` a mitad de frase (el bug de `*/` prematuro en CSS tiene un equivalente en HTML) — ninguna coincidencia accidental.

## Toast de confirmación al eliminar un chat (2026-08-18)

Pedido explícito del usuario: al eliminar un chat, mostrar un toast/snackbar de confirmación en la esquina inferior derecha ("el chat con el nombre tal ha sido eliminado con éxito"). Primer uso de un componente nuevo, pensado para reusarse en cualquier acción futura de este archivo, no solo Delete chat.

- **`showToast(message, { type = 'success' })`** (nueva función genérica, junto a `formatRelativeTime`): crea un `.toast` dentro del portal `#toastContainer`, lo anima a `is-open` en el siguiente frame y lo retira solo (fade-out + `remove()`) a los 4 segundos. `type: 'error'` ya está soportado (ícono `circle-x`, color `--color-danger`) aunque hoy nada lo dispara todavía — solo `deleteChat()` llama `showToast()`, con el default `'success'` (ícono `circle-check`, color `--color-success`).
- **`#toastContainer`** — portal nuevo, hermano de `.app-shell` (mismo criterio que `#chatActionsMenu` y el resto de popovers de este archivo: flota por encima de lo que esté abierto, sin depender de en qué vista esté el usuario). `.toast-container` es `position: fixed; right/bottom: 20px`, `flex-direction: column-reverse` para que un toast nuevo empuje a los anteriores hacia arriba en vez de abajo.
- **Clases sin prefijo `chat-`/`delete-` a propósito** (`.toast`, `.toast-container`, `.toast--success`/`--error`) — es un componente genérico, no algo scopeado a Chats, aunque su primer caller viva en esta vista.
- **`deleteChat(id)`** ahora lee `CHATS_DATA[index].name` antes del `.splice(...)` (que ya lo saca del array) y llama `` showToast(`Chat "${chatName}" deleted successfully`) `` — de forma optimista, igual que el resto de esta función (no espera a que resuelva el `.delete()` contra Supabase). El nombre se inserta con `textContent`, no `innerHTML`, porque puede venir de un rename hecho por el usuario.
- **Bug encontrado y corregido de inmediato, mismo patrón ya conocido en este archivo**: el comentario CSS que documenta `.toast-container` incluía sin querer la secuencia `chat-*/` seguida de `delete-*` — eso cierra el comentario a mitad de frase y silenciosamente descarta todo lo que sigue hasta el próximo punto de recuperación del parser, incluida la regla real de `.toast-container`. Se reprodujo con Chrome headless (`getComputedStyle('#toastContainer').position` daba `static` en vez de `fixed`) y se corrigió reescribiendo el comentario para no formar `*/` accidentalmente. Reverificado: `position: fixed`, `bottom/right: 20px`.
- **Verificado con Chrome headless** (sintaxis del script completo con `node --check`, más un DOM sintético con `showToast` real): el toast muestra el texto exacto con el nombre del chat interpolado, entra a `is-open` (opacidad ~1, transform en su posición final), y el color del ícono computa `rgb(27, 131, 84)` (`--color-success`). No se pudo probar el flujo real de punta a punta (click en "Delete" del modal → `deleteChat` → toast) contra una sesión de Supabase real en este entorno.
- **Border-radius del toast hereda el nuevo estándar de 12px** (`var(--radius-md)`, ver "Delete chat" arriba) — no se le puso un valor propio, reusa el mismo token que ya usan los 8 modales.

## Estándar de altura de botones en modales (2026-08-18)

Pedido explícito del usuario, a raíz de comparar el modal de Delete chat contra el header de esta misma vista: "Cancel" y "Delete" del modal se veían más altos que "Select"/"New chat" del header, **y además distintos entre sí** (Cancel más bajo que Delete, dentro del mismo modal).

- **Causa raíz**: `.btn-primary`, `.btn-secondary` y `.btn-danger` (los 3 primitivos globales, `shared/tokens.css`) no declaran ningún `height` explícito — cada uno depende de su propio `padding` + `font-size` para definir su alto real. `.btn-secondary` (10px→8px de padding vertical, 13px de fuente) siempre da un botón más bajo que `.btn-primary`/`.btn-danger` (10px de padding vertical, 14px de fuente). Como "Cancel" es siempre `.btn-secondary` y la acción principal (`Save changes`/`Delete`/`Create project`) es siempre `.btn-primary` o `.btn-danger`, **todo par Cancel + acción en cualquier modal del prototipo tenía este mismo desajuste** — no era un problema puntual del modal de Delete chat.
- **Ya existía un fix probado para este exacto problema**: `.chats-view-header .btn-primary, .chats-view-header .btn-secondary { height: 32px; padding: 0 14px; }` — normaliza "Select" y "New chat" a los mismos 32px sin tocar los primitivos globales (que otros botones, como los CTA grandes de las vistas vacías de Projects/Apps, siguen usando tal cual). Se reusó el mismo criterio en vez de inventar uno nuevo.
- **Fix aplicado**: una única regla nueva en `flows/home/index.html`, agregada justo después del último `*-modal-footer` existente, que lista explícitamente los botones de los 7 modales con acciones reales (`.project-edit-modal-footer`, `.create-project-modal-footer`, `.delete-project-modal-footer`, `.app-edit-modal-footer`, `.delete-app-modal-footer`, `.rename-chat-modal-footer`, `.delete-chat-modal-footer`) y fija `height: 32px; padding: 0 14px;` sobre `.btn-primary`/`.btn-secondary`/`.btn-danger` dentro de cada uno — mismo valor que ya usa `.chats-view-header`, así "Cancel" y la acción principal quedan siempre a la misma altura entre sí, y esa altura coincide con la de "Select"/"New chat".
- **`.search-modal-footer` queda afuera a propósito** — no tiene botones `.btn-*`, solo hints de teclado (`<kbd>`), así que no aplica esta regla.
- **Por qué no se tocó el primitivo global directamente**: cambiar el `height` de `.btn-primary`/`.btn-secondary`/`.btn-danger` en `shared/tokens.css` habría afectado también los botones CTA grandes de las vistas vacías (`#newProjectViewBtn`, `#newAppViewBtn`, en los headers de Projects/Apps), que hoy usan el tamaño default del primitivo y no fueron parte de este pedido — se prefirió repetir el patrón de override scopeado ya probado, en vez de arriesgar un cambio no pedido en otras pantallas.
- **Estándar hacia adelante**: cualquier modal nuevo que se construya con un footer de acciones (`Cancel` + acción principal) debe agregar su clase `*-modal-footer` a esta misma lista de selectores — no hace falta reinventar la altura, solo sumar el selector. Si en el futuro se decide que TODOS los botones del prototipo (no solo los de modales) deben medir 32px por default, eso es un cambio de escala mayor (afectaría los CTA de Projects/Apps) que debe pedirse explícitamente, no asumirse a partir de este fix.
- **Verificado con Chrome headless** (extrayendo `<style>` real + `shared/tokens.css`, sin pasar por login): `getComputedStyle(...).height` de Cancel/Delete (Delete chat), Cancel/Save changes (Rename chat), Cancel/Delete (Delete project) y Select/New chat (`.chats-view-header`) da **`32px` en los 8 casos**, sin excepción.
- **Fuera de alcance de esta pasada, anotado**: `#newProjectViewBtn`/`#newAppViewBtn` (headers de Projects/Apps) siguen sin este tratamiento — sería el mismo criterio que ya tiene `.chats-view-header`, pero no fue parte de este pedido (que fue sobre modales). Ver "Pendiente / abierto".

## Header fijo, solo la lista hace scroll (2026-08-18)

Pedido explícito del usuario: con muchos chats, el título + buscador/Select/New Chat + tabs All/Archived deben quedarse siempre visibles — solo la lista de abajo debe scrollear.

- **`.chats-view` deja de scrollear como un todo.** Antes era `overflow-y: auto` sobre toda la vista (header incluido) — con una lista larga, el header se iba scrolleando junto con las filas. Ahora es un contenedor flex-column (`overflow: hidden`) del alto completo de `.chat-pane`, con el header y las tabs como hijos `flex-shrink: 0` (nunca se achican ni se van) y una única región scrolleable nueva.
- **`.chats-scroll-area`** (nuevo wrapper alrededor de skeleton/lista/empty, mismos ids sin cambios — `#chatsSkeletonList`/`#chatsList`/`#chatsEmpty` siguen siendo los mismos elementos, solo ganaron un padre) es el único hijo con `flex: 1; min-height: 0; overflow-y: auto` — `min-height: 0` es lo que le permite a un flex child encogerse por debajo de su contenido y scrollear adentro, en vez de forzar a toda la columna (header fijo incluido) a crecer más allá del alto de `.chats-view`.
- **El padding inferior de la vista** (antes `padding: 32px 40px` parejo en las 4 direcciones) se movió al `padding-bottom` de `.chats-scroll-area` — el resto de la vista se quedó con `padding: 32px 40px 0`, así el respiro de abajo viaja con el contenido que scrollea en vez de quedar pegado al borde de un contenedor que ya no tiene scroll propio.
- **Verificado con Chrome headless**, esta vez incluyendo también `shared/tokens.css` (no solo el `<style>` inline de este archivo — la primera pasada de esta verificación se quedó corta porque `.app-shell` vive en tokens.css, y sin esa regla nada quedaba constreñido a la altura de la ventana): con 3000px de contenido simulado en la lista, `.chats-scroll-area` mide 694px visibles de 900px de ventana; tras `scrollTop = 1000`, la posición del header (`getBoundingClientRect().top`) es idéntica antes y después del scroll (32px las dos veces) — confirma que el header no se mueve un píxel mientras la lista sí. No se pudo probar el click/scroll real de punta a punta contra el Supabase real en este entorno.
- **Color del scrollbar de esta nueva región, cambiado a nivel global** (a pedido del usuario, al notar el color al usar `.chats-scroll-area`) — no es un cambio scopeado a Chats, afecta el scrollbar en todo el sitio. Ver `docs/README.md` → historial 2026-08-18 ("Color del scrollbar, cambiado a nivel global") para el detalle completo (nuevo token `--scrollbar-thumb-color` en `shared/tokens.css`, decoupled de `--color-border-strong`).

## Orden de la lista: del más reciente al menos reciente (2026-08-18)

Bug reportado por el usuario: la vista no listaba los chats por recencia. Causa y fix viven del lado de Supabase, no de esta vista — `CHATS_DATA` ya no es mock estático (ver `docs/supabase.md`), lo puebla `loadChatsFromSupabase()`, que hacía `select('*')` sin ningún `.order()`. Sin ordenar explícitamente, Postgres no garantiza devolver las filas por fecha. Fix: `.order('created_at', { ascending: false })` agregado a esa consulta — el resto de esta vista (`getFilteredChats`, `renderChatsList`) no necesitó ningún cambio, ya que preserva el orden del array que recibe. Ver `supabase.md` → "Bug encontrado y corregido" para el detalle completo.

## Click en una fila abre el chat de verdad (2026-08-18)

Hasta esta pasada, clickear una fila fuera del modo selección era un no-op deliberado (documentado como tal — ver "Decisiones tomadas" arriba y `docs/supabase.md` → Pendiente: "Abrir un chat existente desde la vista Chats... sigue siendo un no-op"). Pedido explícito del usuario: que abra ese chat, reusando la vista de hilo que ya existe (`#chatThread`, a todo el ancho, con el botón de panel lateral derecho) en vez de construir una pantalla nueva.

- **`openChatFromList(chat)`** (nueva función, junto a `openExistingChat`): arma el mismo estado de "hilo activo a ancho completo" que ya usan `startChat()`/`openProjectChat()`/`openAppChat()` para entrar a la conversación desde otra vista — oculta Chats/Projects/Apps, muestra `#chatThread`, asegura que **no** quede en el layout partido de panel lateral (`has-panel` removido explícitamente, `setPanelExpanded(false)`, tabs del panel reseteados) porque eso es específico de abrir el propio workspace de un Project/App, no de un chat suelto. El botón de mostrar/ocultar el panel (`chatPanelToggle`) queda disponible como siempre, simplemente no se auto-abre.
- **El breadcrumb del header usa el mismo mecanismo que `openProjectChat`/`openAppChat`**: en vez de enseñarle a `updateChatThreadHeader()` a leer `chat.context` directamente, se escribe `contextState.empty.selected` con la forma `{ type, id, name }` que esa función ya sabe leer (resolviendo el nombre vía `findPanelItem()`, la misma función que ya arma el ícono/nombre de contexto en la fila de esta vista) — cero lógica nueva de resolución de contexto.
- **La carga de mensajes reales reusa `openExistingChat(chat)`** tal cual — el mismo camino que ya usa el popover de historial de un proyecto (Fase 3 de Supabase) para traer `chat_messages` de verdad y fijar `currentChatId`. Un chat viejo sin mensajes guardados simplemente abre con el hilo vacío, legítimamente (no fabrica ningún mensaje de relleno).
- **Nav activo se queda en "Chats" al entrar** — corregido el mismo día, reportado por el usuario. La primera versión copiaba el criterio de `startChat()`/`openProjectChat()` (resaltar "New Chat" para cualquier hilo activo, nuevo o reabierto), pero acá no aplica: el chat se abrió *desde* Chats, así que la sección activa debe seguir siendo Chats, no saltar a "New Chat" como si se hubiera iniciado algo nuevo. `openChatFromList()` marca `.nav-item[data-nav="chats-tasks"]` en vez de `data-nav="new-chat"`.
- **CSS**: la fila deja de tener `cursor: pointer`/hover solo condicionados a `is-select-mode` — ahora aplican siempre, ya que el click hace algo real en ambos modos (abrir vs. seleccionar).
- **Fuera de alcance de esta pasada, gap preexistente sin tocar**: las filas de chat del sidebar (Pinned/Recent) siguen siendo no-op — el pedido fue puntual sobre la lista de esta vista, no sobre el sidebar. Ver `docs/supabase.md` → Pendiente.
- **Sin verificación end-to-end con sesión real**: no hay credenciales de prueba disponibles en este entorno contra el proyecto Supabase real — se validó sintaxis JS y que todas las referencias (`appsView`, `findPanelItem`, `chatPanelToggle`, etc.) existen, más una carga real de la página sin errores de consola (JS no truena), pero no un click real de punta a punta contra datos reales. Confirmar en un navegador con sesión iniciada.

## Estado actual de implementación

- ✅ Vista real reemplaza el empty state genérico para `data-nav="chats-tasks"` — `sectionCopy['chats-tasks']` se eliminó, ya no hace falta.
- ✅ Header título + buscador (mismo mecanismo morph botón↔input que Projects, clases propias)
- ✅ Lista de filas con ícono + nombre + contexto (Project/App/Agent o ninguno) + fecha
- ✅ Selección masiva: checkboxes, contador, Cancel, Archive (única acción real)
- ✅ Ellipsis por fila con "Rename chat", abre un modal real (ver sección abajo)
- ✅ "New Chat" del header reusa el botón real del sidebar
- ✅ Loading skeleton en entrada a la sección y en cada búsqueda (ver arriba)
- ✅ Pin/Unpin sincroniza en ambas direcciones con el sidebar, incluida la ellipsis dinámica por chat pineado ahí (ver sección arriba)
- ✅ Rename chat actualiza `CHATS_DATA` y sincroniza con el sidebar si el chat está pineado (ver sección abajo)
- ✅ Persistencia real vía Supabase (`CHATS_DATA` ya no es mock — ver `docs/supabase.md`); crear/renombrar/pinear/archivar sobreviven a un reload
- ⛔ El concepto de "Task" sigue sin definir — nada en esta vista lo representa todavía
- ✅ Archivar un chat pineado (bulk Archive) también oculta su fila del sidebar Pinned, mismo criterio que el Archive de Projects
- ✅ Tabs All / Archived con su propio filtro, empty state dedicado y ellipsis recortada para archivados (ver sección arriba)
- ✅ Nav item, tooltip y título de la vista dicen "Chats" (ya no "Chats and Tasks")
- ✅ Click en una fila abre ese chat de verdad en `#chatThread` (ver sección arriba), con historial real vía `openExistingChat`
- ✅ Delete chat, activo o archivado, con modal de confirmación y toast de éxito en la esquina inferior derecha (ver secciones arriba)
- ✅ Empty state ilustrado ("No chats yet") cuando la tab All no tiene ningún chat y no hay búsqueda activa, con ícono animado en loop y CTA de "New chat" siempre visible (ver sección arriba)

## Bug encontrado y corregido: el margin-top de la vista no se aplicaba

Al construir la vista, `.chats-view { flex: 1; overflow-y: auto; padding: 32px 40px; }` nunca llegaba a renderizarse — la vista quedaba pegada arriba del todo, sin el padding de 32px que sí tiene Projects. Causa raíz: el comentario CSS que documenta la sección contenía, sin querer, la secuencia literal `*/` dentro del texto (`(.chats-*/.chat-list-row*)`, `.projects-*/.project-list-row`) — eso cierra el comentario a mitad de frase, y todo lo que sigue hasta el siguiente punto de recuperación del parser (incluida la regla `.chats-view` real) queda corrompido y se descarta silenciosamente. Se reprodujo con Chrome headless real (no solo lectura de código): `getComputedStyle('#chatsView')` mostraba `flex: 0 1 auto` y `padding: 0px` (los valores por defecto, como si la regla no existiera). Fix: reescrito el comentario para no formar `*/` accidentalmente (`.chats-*` y `.chat-list-row*` en vez de `.chats-*/.chat-list-row*`). Verificado de nuevo con Chrome headless: `flex: 1 1 0%`, `padding: 32px 40px`, altura completa. **Lección para el resto del prototipo**: cualquier comentario CSS que mencione selectores con `*` (wildcard/prefijo) separados por `/` puede formar `*/` sin querer — revisar antes de escribir ese patrón en un comentario.

## Pendiente / abierto

- **Definir qué es una Task** y cómo convive con los Chats en esta misma vista (¿tipo de ítem distinto con su propio ícono/status, o una etiqueta sobre un chat existente?).
- **"Rename chat" propagado a Recent/Search**: el nombre editado se propaga a `CHATS_DATA` y al sidebar Pinned si el chat está ahí, pero no a `SEARCH_DATA.chats` (Cmd+K) ni al markup estático de Recent — esos siguen siendo mocks independientes.
- **¿Archivar un chat acá debería reflejarse en Recent/Search?** Hoy no toca esos dos — solo el sidebar Pinned (si estaba ahí). Mismo tipo de brecha ya documentada entre `PROJECTS_DATA` y el markup estático de Pinned.
- **`.project-actions-menu` (Projects) tiene el mismo bug de clipping que se corrigió acá, sin corregir todavía** — vive `position: absolute` anidado dentro de `.projects-list`, que también tiene `overflow: hidden` (mismo motivo: esquinas redondeadas). No fue parte de este pedido (que fue puntual sobre la vista Chats), pero queda anotado para no perderlo — el mismo fix de portal aplicaría 1:1.
- **La vista Projects tiene el mismo problema de "header que scrollea junto con la lista"**, sin corregir — `.projects-view` sigue siendo `overflow-y: auto` sobre toda la vista, título/buscador/Filter/tabs de ownership incluidos. Mismo fix (header fijo + `.projects-scroll-area`) aplicaría 1:1 si se pide.
- **`#newProjectViewBtn`/`#newAppViewBtn` (headers de Projects/Apps) no tienen el override de 32px** que sí tienen `.chats-view-header` y ahora todos los modales (ver "Estándar de altura de botones en modales" arriba) — mismo criterio aplicaría 1:1, pero no fue parte de ningún pedido sobre esas vistas todavía.

## Archivos relacionados

- `flows/home/index.html` — markup (`#chatsView`, `#chatsEmptyHero`, `#toastContainer`, los modales `#renameChatModalBackdrop`/`#deleteChatModalBackdrop`), estilos inline (`.chats-*`, `.chat-list-row*`, `.chat-actions-menu`, `.chats-empty-hero`/`.chats-empty-orbit`/`.chats-empty-card`/`.chats-empty-core`, `.toast*`), y el script: `CHATS_DATA`, `getFilteredChats`, `renderChatsList`, `applyChatsFilters`, `loadChatsView`, `setChatsSelectMode`, `toggleChatSelected`, `archiveSelectedChats`, `archiveChat`, `unarchiveChat`, `deleteChat`, `openDeleteChatModal`, `toggleChatPinned`, `renameChat`, `openChatFromList`, `showToast`, `wireChatsListDelegation`, `initChatsView` — reusa `findPanelItem`/`iconForItem`/`PANEL_OPEN_ICONS` ya definidos para el picker del panel lateral y el selector de contexto.
- [`sidebar.md`](./sidebar.md) — nav item "Chats and Tasks", ahora resuelto (ver su propia sección "Pendiente / abierto").
- [`home.md`](./home.md) — vista Projects, el patrón visual/de header que esta vista replica.
